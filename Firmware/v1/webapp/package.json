{
  "name": "ll-mirror-webapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/gzip-dist.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.22.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
