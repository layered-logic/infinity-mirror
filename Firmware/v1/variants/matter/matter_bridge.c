/*
 * Matter cluster → state bus bridge (Pro+ only).
 *
 * Scaffold. Real implementation registers callbacks for On/Off, Level
 * Control, and Color Control clusters, each posting an LL_EV_* event
 * on the state bus. See firmware-spec.md §5.2.
 */

/* Intentionally empty in the scaffold. */
