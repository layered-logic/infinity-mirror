---
title: Spring 2026 Sprint Log
type: tracking
quarter: Spring 2026
status: active
tags: [sprint, log, progress]
---

# Sprint Log — Spring 2026

Progress tracker for the Infinity Mirror sprint plan. Organized by sprint week.
Each entry is a human-written description with a date and an inline `[LL-NNN]`
link into the [task registry](tasks.md). The registry is the structured source
of truth for task identity, status, and history; the log is the daily narrative.

Format spec: [docs/task-format-v2.md](docs/task-format-v2.md). Append-only event
history is tracked separately in [task_log.md](task_log.md).

---

## Week 1 (Mar 30 – Apr 4): Competitive Landscape & Stakeholders

### Done
- [x] [LL-001](tasks.md#LL-001) LLC Certificate of Formation filed (Mar 23, pre-quarter)
- [x] [LL-002](tasks.md#LL-002) EIN received from IRS (Apr 2) — 41-5282028
- [x] [LL-005](tasks.md#LL-005) Operating agreement — finalized, printed, and signed (Apr 6)
- [x] [LL-003](tasks.md#LL-003) Pricing calculator MVP built — Streamlit dashboard with dynamic BOM, size scaling, tier/platform selection, and margin analysis (`tools/pricing/`) (Apr 2)
- [x] [LL-004](tasks.md#LL-004) Stakeholder mapping exercise completed (Apr 2) — 45-min coached sprint with Gemini acting as investor/board member. Produced comprehensive 5-phase stakeholder map covering 24 stakeholders across Creation, Distribution, Acquisition, Lived Experience, and Support/Legacy. Formalized as [docs/stakeholder-map.md](docs/stakeholder-map.md).

- [x] [LL-006](tasks.md#LL-006) ESP32 module selected — ESP32-C3-MINI-1 (Apr 6). Chosen for smallest footprint, lowest cost (~$2 LCSC), wide availability, and built-in USB Serial/JTAG (no external UART chip needed on PCB). Researched all current Espressif modules; full comparison in [docs/esp32-module-comparison.md](docs/esp32-module-comparison.md), shortlist analysis in [docs/esp32-module-shortlist.md](docs/esp32-module-shortlist.md). Note: ESP32-C6-MINI-1 (adds Zigbee/Thread/Matter) flagged as a potential future SKU variant — decision deferred to customer research (survey early buyers on smart home usage).

### In Progress

### Done (continued)
- [x] [LL-008](tasks.md#LL-008) Business bank account application submitted (Apr 9) and approved (Apr 14). NAICS 335129 (Other Lighting Equipment Manufacturing), calendar fiscal year (12/31), established 03/2026.

- [x] [LL-007](tasks.md#LL-007) Market analysis — competitive landscape research completed (Apr 6). Cross-platform analysis of 30+ listings across Etsy, Amazon, Pinterest, and commercial signage vendors. Produced [docs/Market Analysis Report.md](docs/Market%20Analysis%20Report.md). Key findings: market is bifurcated into 4 segments (Bespoke Luxury $800–$5k+, B2B Branding $250–$1.2k, Sensory/Therapy $230–$4.3k, Lifestyle/Gaming $40–$250). The $50–$120 bracket has 1,900+ Etsy listings — saturation risk if industrial design doesn't signal $200+ value. Matter/Thread protocol fragmentation flagged as a product risk for wi-fi-only devices. Repairability legislation (EU + WA/CA) is both a compliance requirement and a differentiator. FCC Part 15 compliance noted for LED driver EMI.

### Blocked

---

## Week 2 (Apr 7 – Apr 11): Pricing & The Sustainability Audit

### Done
- [x] [LL-009](tasks.md#LL-009) Sustainability audit completed (Apr 9). Full 7-stage cradle-to-grave lifecycle analysis produced as [docs/sustainability-audit.md](docs/sustainability-audit.md). All power data measured via smart plug (6,368 samples): idle 0.1W, rainbow cycling 1.25W avg, full white 2.8W (firmware-capped at ~29% of WS2812B rated max). Annual energy at worst-case usage: 4.91 kWh (typical) – 10.34 kWh (peak). LED lifespan projected at 14+ years. Embodied carbon ~2.5–2.7 kg CO2 cradle-to-door. Key hotspots: coated acrylic panels (non-recyclable at EOL), PSU oversizing (25W brick at 11% load — flagged for v2 right-sizing). Cardboard packaging and PETG separability are sustainability strengths.

- [x] [LL-010](tasks.md#LL-010) BOM breakdown completed (Apr 9). Full material + labor + fixed cost analysis at 1/50/100/1,000 units for basic 6x6 model. Produced [docs/bom-breakdown-basic-6x6.md](docs/bom-breakdown-basic-6x6.md). Key findings: variable COGS ranges from $81.62 (1 unit) to $49.80 (1,000 units); labor is the dominant cost at all volumes (exceeds materials). $50 retail only reaches $60/hr effective rate at 1,000 units — $65 recommended as base price for early volumes. Fixed capital (laser + printer) totals $7,153.11.

- [x] [LL-011](tasks.md#LL-011) JLCPCB PCB cost analysis (Apr 9). Analyzed basic STM8 PCB BOM (7 component lines, 9 parts, 40 joints). JLCPCB estimate: $3.61/board at qty 5, $1.04/board at qty 50, $0.90/board at qty 100. Pricing_config.yaml $5.00 controller placeholder is significantly overstated — actual assembled PCB is ~$1/board at production volume. Extended parts: STM8 (C52717) + USB-C connector (C456012) each add $3 setup. TTP223 capacitive touch sensor evaluated as potential add-on: ~$0.10/board all-in, basic part (no extended fee), senses through PETG at 1–2mm. ESP32 PCB BOM pending refresh.

### In Progress

### Blocked

---

## Week 3 (Apr 14 – Apr 18): Operations & Critical Path

### Done
- [x] [LL-012](tasks.md#LL-012) Factory operations planning completed (Apr 14). Full made-to-order workflow mapped in an external Claude session. Produced three artifacts in `Assembly_docs/`:
  - `LED_Mirror_Project_Brief.md` — full context summary of all decisions made
  - `LED_Mirror_Station_Details.csv` — 20-station production line detail (tools, fixtures, consumables, inputs/outputs, QC criteria per station)
  - `LED_Mirror_Assembly_Calculator.xlsx` — per-unit time calculator with 3 sheets: Time Standards, Unit Calculator, Batch Planner

  Key decisions: 3 parallel production lanes (Frame, Mirrors, Electronics) feeding final assembly; batch-by-operation strategy (not one-unit-start-to-finish); milled mirror channels (not spacer layup); pre-test LED strips at receiving, not mid-build; print a full week of 3D parts in one overnight run; physical traveler card per unit through all 20 stations. Garage workshop layout defined: wood dust zone on one end, electronics bench on the other, pack & ship fully isolated. Phase 1 tooling list finalized (Hakko FX-888D, Dewalt 12" sliding miter saw, 7" wet tile saw, Dewalt 1-3/4hp router, bench PSU, Fluke 115). Solder operations flagged as first bottleneck at scale (two per unit: LED connector and controller PCB — first outsource candidate).

### In Progress

### Blocked

---

## Week 4 (Apr 21 – Apr 25): Brand Identity & Repairability

### Done
- [x] [LL-013](tasks.md#LL-013) Brand positioning worksheet drafted and substantially locked (Apr 20). Full 9-section worksheet produced as [docs/brand-positioning.md](docs/brand-positioning.md). Decisions locked:
  - **Architecture:** three-tier (Layered Logic parent → product mini-brand → product lines; consulting practice also lives under the Layered Logic umbrella)
  - **Audience priority:** Interior Curator > Vibe-Seeker >> Spec-Head with "tech disappears" founding principle
  - **Five adjectives:** Ambient, Considered, Quiet, Crafted, Living
  - **Voice:** first-person "I" while solo-founder; transition to "we" as team grows
  - **Typography:** Neue Haas Grotesk ExtraLight Italic as *default posture* with upright reserved for emphasis and gear-shift moments. Berkeley Mono as technical accent (ABC Diatype rejected for its "R" tail; JetBrains deprioritized)
  - **Palette:** Hybrid three-tier — structural/editorial layer (near-black `#0B0A0F`, warm cream `#F4EFE6`, mid grey `#8A8A8E`, Indigo Deep `#150A4A`) + brand anchor (Indigo Reference `#4A25FF`, Indigo Signal `#3214FF`, Indigo Stone `#4F3FB0`) + glow spectrum restricted to gradients/motion (Indigo→Cyan, Indigo→Pink). *Originally locked as magenta-violet Apr 20; shifted to indigo Apr 21 after rendered artifacts read too pink.*
  - **Tagline:** *"Light that layers."* for the infinity mirror line; `"[X] that layers."` as parent-brand motif extending to other product lines and the consulting arm (*"data that layers"*)
  - Companion research doc [docs/brand-research-notes.md](docs/brand-research-notes.md) analyzes five moodboard references (Paris Electronic Week, SPRING/LAVA, Intrinsic/Pentagram + Felix Faire, IBM Quantum/Field.io) and distills five signature visual devices
  - Still open: §1 positioning sentence final edit, mini-brand name (Tier 2, deferred)

- [x] [LL-014](tasks.md#LL-014) Palette swatches produced (Apr 20) as SVGs in [assets/brand/color/](assets/brand/color/): Option B (LED spectrum, reference), Option C (editorial warm, reference), and Hybrid (locked). Hybrid swatch includes in-situ dark/light mode previews with italic tagline and mono callout.

- [x] [LL-015](tasks.md#LL-015) Repository restructured as an Obsidian vault (Apr 20). YAML frontmatter + `## Related` cross-linking added across README, sprint_plan, sprint_log, all `docs/` content. MOC established at [index.md](index.md) as the home note. Standard markdown links (not wikilinks) chosen for GitHub compatibility.

- [x] [LL-016](tasks.md#LL-016) Design assets folder scaffolded (Apr 20) at [assets/](assets/) with subfolders for brand (color/logo/type/tokens), moodboards, product-photos, icons, figma, source-files. Conventions documented in [assets/README.md](assets/README.md).

- [x] [LL-017](tasks.md#LL-017) Layered Logic logo P5.js repo reviewed (Apr 20). Bill is developing the nested-L mark independently at [layered-logic/layered-logic-logo](https://github.com/layered-logic/layered-logic-logo) — mouse-reactive histogram bars on two nested L paths with Gaussian quantization and corner-blending. First-pass review captured in [brand-positioning §7](docs/brand-positioning.md) with 5 parked items (color unification with brand palette, canonical rest pose, favicon variant, horizontal-arm asymmetry, optional indigo bloom layer). Logo work is out of scope this sprint — parked until Bill reopens the track.

- [x] [LL-019](tasks.md#LL-019) Advisor meeting doc drafted (Apr 21). [docs/Advisor-Meetings/advisor-meeting-2026-04-21.md](docs/Advisor-Meetings/advisor-meeting-2026-04-21.md) — M1 close-out, Week 4 brand/engineering deliverables summary, 5 open questions staged for advisor (brand scope overreach, greenfield firmware scope, right-to-repair liability stress-test, user research rigor, PCB production order timing).

- [x] [LL-020](tasks.md#LL-020) Logo color decision resolved + full SVG set exported (Apr 21). Parked item #1 from logo-spec closed: pulled the mark into the brand palette. Initial resolution used magenta-violet `#BF00FF`/`#A020D0`, but rendered artifacts read too pink, so the whole palette was shifted to indigo the same afternoon. **Final values:** outer L `#4A25FF` (Indigo Reference), inner L `#3214FF` (Indigo Signal). Three deterministic SVG exports: [logo-primary-dark.svg](assets/brand/logo/logo-primary-dark.svg), [logo-primary-light.svg](assets/brand/logo/logo-primary-light.svg), [logo-primary-mono.svg](assets/brand/logo/logo-primary-mono.svg). P5 source still runs `#420AFF` on the outer — minor sync (`→ #4A25FF`) parked with the logo track; inner already matches.

- [x] [LL-021](tasks.md#LL-021) Palette shifted magenta-violet → indigo (Apr 21). After the logo color resolution rendered artifacts in `#BF00FF`/`#A020D0`, the magenta cast read too pink. Palette re-anchored on indigo: Reference `#4A25FF`, Signal `#3214FF`, Stone `#4F3FB0`, Deep `#150A4A`. Cascade across the whole vault: [brand-positioning.md](docs/brand-positioning.md) §Color Anchor ladder + Five Signature Visual Devices + Hybrid palette block, [brand-research-notes.md](docs/brand-research-notes.md) §Five Visual Devices + three moodboard "avoid" notes, [palette-hybrid-recommended.svg](assets/brand/color/palette-hybrid-recommended.svg) re-derived and now includes the full four-rung ladder, [palette-option-c-editorial-warm.svg](assets/brand/color/palette-option-c-editorial-warm.svg) recolored (retained as rejected-alternative record), [palette-option-b-led-spectrum.svg] deleted (redundant with the hybrid's glow-spectrum block), logo SVGs re-rendered with the indigo anchors, [firmware-architecture-scoping.md](docs/firmware-architecture-scoping.md) default-color updated and confirmed, [advisor-meeting-2026-04-21.md](docs/Advisor-Meetings/advisor-meeting-2026-04-21.md) brand summary + §5.4 Q updated, memory file updated. P5 source still renders `#420AFF` on the outer — minor sync (`→ #4A25FF`) parked with the logo track.

- [x] [LL-022](tasks.md#LL-022) External chat archive converted to markdown (Apr 21). Three Gemini JSON exports (market research + repo sharing, bank account recommendation) converted to paired `.md` files with YAML frontmatter, summaries, and tags for Obsidian vault integration. The earlier Apr 2 "Sharing GitHub Repos" export flagged `status: superseded` since it's a partial of the Apr 6 "Market_Research" export of the same thread. [LED_Mirror_Project_Brief.md](external_chats/LED_Mirror_Project_Brief.md) retrofit with frontmatter + summary. [external_chats/README.md](external_chats/README.md) added as folder index. [convert_chats.py](external_chats/convert_chats.py) is the reusable converter — hand-written summaries are configured inside the script (they require reading each thread).

- [x] [LL-023](tasks.md#LL-023) Palette swatch contrast fixes + advisor doc condensed (Apr 21). Ran WCAG contrast audit on indigo type in both surviving swatches: `#3214FF` on `#0B0A0F` fails AA at ~2.5:1. Fixes applied to [palette-hybrid-recommended.svg](assets/brand/color/palette-hybrid-recommended.svg) and [palette-option-c-editorial-warm.svg](assets/brand/color/palette-option-c-editorial-warm.svg): section headers swapped to cream `#F4EFE6` (~14:1, AAA); mono tick-mark labels on dark ground swapped to lighter indigo tint `#8E7FFF` (~6.2:1, AA large); mono labels on cream ground left as `#3214FF` (passes AAA on cream). Hybrid swatch also: background rect extended to full viewBox (was 60px short); light-mode bloom opacity boosted `0.5 → 0.6` (~20% stronger glow). Advisor meeting doc [advisor-meeting-2026-04-21.md](docs/Advisor-Meetings/advisor-meeting-2026-04-21.md) rewritten for concision — trimmed from ~140 to ~85 lines while preserving all 5 open questions and deliverable summary.

- [x] [LL-025](tasks.md#LL-025) **Button interface design doc drafted + locked (Apr 22).** Two-button hardware model locked in [docs/button-interface.md](docs/button-interface.md): primary exposed button for daily UI (single = on/advance base color, double = change pattern, triple = advance brightness, hold = off), recessed pinhole button for rare ops (short-hold 3s = BLE pairing, long-hold 10s = factory reset). Inherits V0 prototype's single-button interaction grammar for continuity; adds recessed button to keep destructive/configuration actions behind a "you meant this" physical barrier (Eero convention). Three UX decisions resolved in same session: color cycle = 16 HSV spectrum steps + 1 white (17 total, independent of brand palette), brightness = 4 discrete steps (25/50/75/100%), recessed button = hold-only (no tap gestures). Persistence model: soft-off (hold) resumes prior state, hard-off (power loss) resumes to default color. Doc also locks state-machine, LED feedback cues, and GPIO assignment plan (primary stays on GPIO9 from V0; recessed TBD at PCB layout). One minor refinement parked for user testing: whether self-palette patterns should flash briefly on single-press to acknowledge the base-color change.

- [x] [LL-026](tasks.md#LL-026) **WA BLS registration filed (Apr 22).** Submitted Business Licensing Service application with WA Dept. of Revenue. Activities registered: Retailing (primary, 0.471% B&O) + Manufacturing (secondary, 0.484% B&O) — MATC covers the overlap. NAICS 335129. $90 business license fee paid. UBI issued at submission; physical license arrives by mail in ~10 business days. Business description: "Design, manufacture, and online retail sales of custom LED infinity mirror art and related lighting products." Follow-ups: (1) reseller permit application (separate, free, 4-yr validity) once UBI is confirmed — saves sales tax on component purchases from WA suppliers; (2) verify no city endorsement gap depending on business address; (3) first B&O return due quarterly or annually based on revenue threshold assigned by DOR.

- [x] [LL-027](tasks.md#LL-027) **Firmware architecture block locked (Apr 22).** Full Wed block of the sprint plan — all 5 architecture decisions from [firmware-architecture-scoping.md](docs/firmware-architecture-scoping.md) §5 resolved. Scoping doc updated in-place with 🔒 Decision blocks capturing each lock + rationale; original options/tradeoffs preserved for traceability. Decisions:
  - **§5.1 Language/framework:** ESP-IDF with C (not C++). Small C++ islands acceptable where RAII helps (pattern object lifecycle), but C is the default. Picked for paste-and-go from ESP-IDF's ~95%-C example base and clean FFI boundaries.
  - **§5.2 Provisioning:** BLE primary + SoftAP fallback, both via Espressif's `wifi_prov_mgr`. **Radios dark at rest** — no passive advertising. User-initiated pairing only, triggered by 3s hold on recessed button. Consistent with "mirror is a light first" principle — smart-device pairing is explicit opt-in, not a boot-time dance.
  - **§5.3 Smart-home integration:** **Two firmware variants, shared core library.** Pro (no Matter, C3-MINI-1 target, 4MB flash) + Pro+ (uncertified Matter with test VID 0xFFF1, C6 target, 8MB flash). Matter stays off by default on Pro+ (user opts in). Full three-tier product line taxonomy locked: Basic (STM8) / Pro (ESP32 + LL app + webapp + HA integration) / Pro+ (Pro + Matter). Matter architectural rules: On/Off + Level Control + Color Control clusters only; all controllers write to a unified state bus. Demo units ship on ESP32-C6-DevKitC-1 for both variants.
  - **§5.4 OTA:** Self-hosted HTTPS on Cloudflare Worker + R2 at `ota.layeredlogic.cc`. ECDSA P-256 signed binaries (private key offline in password manager, public key in bootloader). A/B partitions with `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` (auto-revert if new build fails health check within 60s) + `CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK` (monotonic version counter in eFuse — refuses downgrades). Server-side staged rollout: 1% → 10% → 50% → 100% via device-ID hash buckets. **Opt-in telemetry** where opt-in cohort gets builds first (de-facto beta); opt-outs only get builds after stability proven. OTA checks on boot + every 24h with device-specific hash-based time offset + ±30min jitter (no fleet-wide sync, no consistent overlap with user network routines).
  - **§5.5 App protocol:** BLE GATT (provisioning + fallback) + WebSocket over Wi-Fi (primary, broadcasts to all clients) + HTTP REST (one-shot ops). JSON envelope with `op`, `req_id`, `ts`, `payload`, `hmac`. mDNS discovery on `_layeredlogic._tcp.local.` with BLE fallback when device is in pairing mode or Wi-Fi down — **no manual IP entry**. **User-selectable auth at provisioning:** Open (default, shared-space friendly) or Paired (HMAC-signed messages). Rationale: in a shared space a roommate should be able to turn off the mirror without credentials; the user knows their situation best. Device hosts the webapp in V1; hosted webapp at `layeredlogic.cc/controlmydevice` deferred (mixed-content HTTPS→WS blocker).

  Downstream spec docs drafted in same block: [firmware-security.md](docs/firmware-security.md) (signing, auth, OTA, telemetry), [control-protocol-spec.md](docs/control-protocol-spec.md) (shared wire protocol), [firmware-spec.md](docs/firmware-spec.md) (ESP32 component spec across both variants), [app-spec.md](docs/app-spec.md) (LL mobile app), [webapp-spec.md](docs/webapp-spec.md) (device-hosted webapp + V2 hosted plan). Scoping doc added new §6 Variant Architecture and §7 Board Parameterization — `firmware/core/` shared library, `firmware/variants/{standard,matter}/` thin layers, `firmware/boards/{c6_devkit,c3_devkit,prod_v1_pro,prod_v1_pro_plus}.h` abstract hardware differences via compile-time `LL_PIN_*` + `LL_HAS_*` flags. Post-sprint parked: fleet telemetry system design (opt-in, minimal, transparent).

- [x] [LL-028](tasks.md#LL-028) **BOI Report — confirmed exempt (Apr 22).** Checked [fincen.gov/boi](https://fincen.gov/boi); site banner reads: *"All entities created in the United States — including those previously known as 'domestic reporting companies' — and their beneficial owners are now exempt from the requirement to report beneficial ownership information (BOI) to FinCEN."* Layered Logic LLC (WA-formed, Mar 23, 2026) is a US-domestic entity and qualifies. No filing required under the current FinCEN interim final rule. Sprint plan updated. Calendar note: re-check FinCEN guidance before Jun 21 original deadline in case the rule is reversed or narrowed.

- [x] [LL-024](tasks.md#LL-024) Brand vault interlink pass (Apr 21). Typography rationale pulled out of brand-positioning §5 into its own doc at [docs/typography-decisions.md](docs/typography-decisions.md) — covers italic-default inversion, why NHG over Inter/GT America/Söhne, why Berkeley Mono over JetBrains/Plex/Diatype Mono, licensing. Logo §7 similarly delegated to a dedicated spec in [assets/brand/logo/](assets/brand/logo/) with three artifacts: [logo-spec.md](assets/brand/logo/logo-spec.md) (branding + constraints + 5 parked items), [logo-code-notes.md](assets/brand/logo/logo-code-notes.md) (P5 parameters + architecture + dev recipes), and [logo-primary-dark.svg](assets/brand/logo/logo-primary-dark.svg) (deterministic rest pose derived from P5 params — resolves parked item #2). Moodboards and type folders scaffolded with READMEs ([assets/moodboards/README.md](assets/moodboards/README.md) with 5 reference links + summaries, [assets/brand/type/README.md](assets/brand/type/README.md) with what-belongs-here guidance). Cross-links tightened across all docs.

- [x] [LL-029](tasks.md#LL-029) **Firmware v1 bring-up — first four core modules + host test harness (Apr 22).** Greenfield ESP-IDF firmware moves from blink-level scaffold to a runnable end-to-end loop. Modules landed under [Firmware/v1/core/](Firmware/v1/core/):
  - **`state_bus/`** — single source of truth for runtime state (on, brightness, base_color_rgb, pattern_id, led_count). Owns a dedicated `esp_event` loop on its own task to enforce single-writer concurrency. `LL_EV_STATE_CHANGED` (0x1000) is the broadcast event; transient events at 0x1001+ pass through without state mutation (covers recessed-hold begin/end and future provisioning/factory cues).
  - **`button/`** — debounced GPIO ISR + FreeRTOS task driving two gesture state machines: primary button (single/double/triple/hold, 600ms hold, 200ms multi-press window, 20ms debounce — values inherited from canonical STM8) and recessed button (3s = provision, 10s = factory reset). Pure logic split into `button_logic.[ch]` for host testing; ISR/task glue stays in `button.c`. Recessed press posts `LL_EV_RECESSED_HOLD_BEGIN/END` for live feedback.
  - **`led_driver/`** — ESP-IDF RMT-backed WS2812B driver. Custom compound encoder composes `bytes_encoder` (T0H=4, T0L=8, T1H=8, T1L=4 ticks @ 10MHz / 100ns per tick) + `copy_encoder` (500-tick reset). Pure C `led_driver_encode_frame()` does GRB reorder + brightness scaling (0–100% clamped, integer math) — fully host-testable, separated from RMT.
  - **`pattern_interp/`** — render task pinned to core 1 at priority +3, 60Hz frame loop. Owns 7 stateless renderers (`solid`, `rainbow`, `scanner`, `spinner`, `random`, `breathing`, `twinkle`) implementing [pattern-dictionary.md](docs/pattern-dictionary.md) §3. All animation derived from `tick_ms - pattern_start_ms` so pattern changes reset cleanly with zero allocation. Cue overlay state machine in `cue_logic.c` handles recessed-hold blue blink (0.5s on/off) and factory-reset red flash → 1s green hold; cues bypass user brightness so feedback is always visible. Subscribes to state_bus and drives led_driver. Standard variant's [main.c](Firmware/v1/variants/standard/main.c) now wires state_bus → pattern_interp → button in order; on first power-on the device responds to all primary gestures and recessed feedback.

  Reference docs locked in same block to make the firmware behavior portable across SKUs:
  - **[docs/pattern-dictionary.md](docs/pattern-dictionary.md)** (new) — cross-firmware visual contract: 7 patterns × 13 colors × tick cadence × white-only dimming rule (217/255 factor on solid white only). Defines the wire-format pattern IDs and the porting source-of-truth (STM8 `stm8_150mm.ino`).
  - **[Firmware/STM8/spec.md](Firmware/STM8/spec.md)** (new, 273 lines) — canonical reference for the shipping STM8 Basic firmware. Documents the gesture timings, state machine, pattern behavior, and color palette as **product contract** for all future Layered Logic firmwares to honor.
  - **[docs/firmware-spec.md](docs/firmware-spec.md), [docs/control-protocol-spec.md](docs/control-protocol-spec.md), [docs/button-interface.md](docs/button-interface.md)** — updated to point at the new dictionary, lock V1 patterns as built-in (V2 = uploaded declarative), and reflect the canonical timings.

  **Host test harness** at [Firmware/v1/tests/](Firmware/v1/tests/) — plain CMake + gcc, no ESP-IDF. Covers `button_logic`, `led_driver_logic`, `patterns`, and `cue_logic` (~50 tests across 8 suites). On-target glue (esp_event handlers, RMT, GPIO ISR, render task) deferred to vendored Unity + on-target test app when coverage gap matters.

  **Dev toolchain installed end-to-end on the dev machine (Apr 22):** ESP-IDF 5.5.4 at `C:\Espressif\frameworks\esp-idf-v5.5.4\` (per-session activation via `export.ps1`/CMD/sh — saved as memory `reference_esp_idf_setup.md`); MSYS2 MinGW64 gcc/cmake at `C:\msys64\mingw64\bin\` on permanent PATH for host tests. Nothing has been compiled or flashed yet — first action next session is verification (`cmake -S tests -B tests/build -G "MinGW Makefiles" && ctest` + `idf.py -DLL_VARIANT=standard -DLL_BOARD=c6_devkit build`). Phase 2 firmware now has a real bring-up surface to iterate on; next modules to add are NVS persistence and transport (WebSocket/HTTP).

- [x] [LL-030](tasks.md#LL-030) **Right-to-Repair philosophy doc finalized (Apr 27).** Status updated from draft to complete. The doc at [docs/right-to-repair-philosophy.md](docs/right-to-repair-philosophy.md) covers: core principle, three-part rationale (pro-consumer → environmental → legal), concrete repair commitments (8 items), honest limits (acrylic EOL, ESP32 obsolescence, closed firmware, solo support), compliance reinforcement (EU RtR, WA HB-1933, CA SB-244), and buyer ask. Cross-linked to the new failure mode inventory.

- [x] [LL-031](tasks.md#LL-031) **Failure-mode inventory completed (Apr 27).** 17 individual failure mode files created in [Failure_Modes/](Failure_Modes/), covering all v1.0 components across 8 categories: LED strip (3), PSU (2), controller/PCB (4), acrylic panels (2), wood frame (1), PETG parts (1), wiring/connections (2), buttons (2). Each file documents symptoms, likely causes, probability, step-by-step repair path, tools and parts required, difficulty/time estimates, when to contact support, and design mitigations. Summary: 12 fully user-repairable, 5 partially (require soldering or firmware reflash), 0 non-repairable. Index at [Failure_Modes/README.md](Failure_Modes/README.md). Validates the philosophy doc's claim that every failure mode has a user-serviceable path.

### In Progress
- [LL-042](tasks.md#LL-042) User Repair Guide — customer-facing
- [LL-043](tasks.md#LL-043) User interview script + contact list — outline drafted Apr 28 (see Week 5); contact list still pending

### Blocked
- [LL-044](tasks.md#LL-044) Packaging QR code — laser-etch or perforate into outer cardboard in the same cutting pass; zero extra material, surfaces the repair philosophy at unboxing rather than hiding it on a marketing page. **Blocked Apr 28** — needs final packaging design (cardboard stock, dimensions, print/laser process) decided first. Concept stays parked until packaging is far enough along to know what surface the QR lives on.

### Scheduled — Wednesday Apr 22 focus work day
- [LL-028](tasks.md#LL-028) ~~**BOI Report** filing with FinCEN~~ — **resolved as N/A / exempt Apr 22.** See Done entry below.
- [LL-026](tasks.md#LL-026) ~~**Sales tax permit** — register with WA Dept. of Revenue~~ — **filed Apr 22.** See Done entry below. Reseller permit follow-up still pending once UBI confirmed.
- [LL-027](tasks.md#LL-027) ~~**ESP32 firmware architecture block**~~ — **completed Apr 22.** See the "Firmware architecture block locked" entry below.

### Apr 20 end-of-day
- Firmware architecture scoping doc produced at [docs/firmware-architecture-scoping.md](docs/firmware-architecture-scoping.md). Locks 5 design principles (standalone out-of-box, app is pattern editor, configurable LED count, brand-palette default, closed-binary + self-hosted OTA) and stages 5 open decisions for Wed with options/tradeoffs for each. Also captures: feature-parity checklist extracted from the ESPHome YAML, success criteria for Wed, and explicit out-of-scope items (Matter/Thread SKU decision, app UI design, webapp stack, test harness).
- Clarified product-line scope: 6×6 (32 LEDs) is the shipping product; 12×12 (66 LEDs) is a firmware test prototype only, not a second SKU. Sustainability audit + BOM remain 6×6-only.

---

## Week 5 (Apr 28 – May 5): App UX/UI Design + App Demo Mini-Sprint

### Done
- [x] [LL-008](tasks.md#LL-008) **Business banking fully operational (Apr 28).** External personal accounts linked to the Layered Logic LLC business account and initial funding deposit cleared. Closes the Week 1 banking thread — account is now active and past the inactivity/fee threshold, no risk of closure. Pairs with the Apr 14 account approval.

- [x] [LL-032](tasks.md#LL-032) **User interview outlines drafted (Apr 28).** Lightweight outlines for two audiences — end-buyer (~22 min) and showcase partner / coffee shops (~22 min) — at [docs/user-interview-outline.md](docs/user-interview-outline.md). Captures the data concepts to collect rather than a verbatim script (Bill conducts impromptu). End-buyer flow walks space/taste → shopping behavior → product reaction → customization appetite → control preferences → values alignment → discovery. Coffee-shop flow walks current practice → artist-selection criteria → economics → logistics → fit → referrals. Doc closes with a 7-row "data we want from every conversation" table tying answers back to specific open decisions (price brackets, parked native/webapp call, Pro vs Pro+ SKU split, repair-messaging value).

- [x] [LL-033](tasks.md#LL-033) **Button Design Rationale drafted (Apr 28).** HCDE portfolio companion to [button-interface.md](docs/button-interface.md) at [docs/button-design-rationale.md](docs/button-design-rationale.md). Status: draft, pre-user-research — revises after Week 5 interviews. 10 sections covering: the question (light vs smart device), audience priority (Curator > Vibe-Seeker >> Spec-Head), the core "light first" tension, four rejected alternatives (app-only, single-button-only, capacitive touch, encoder), why two buttons + Eero precedent for the recessed button, why V0's gesture grammar was preserved, trade-offs explicitly accepted (sparse hardware UI, no combos, bounded discoverability, app-survival risk), six testable hypotheses tied to interview questions, and open/parked items. One-sentence summary: *"Two buttons because the mirror is a light first and a smart device second — and the difference between those two roles deserves a physical seam."* Closes the Week 5 sprint plan "design rationale" deliverable.

- [x] [LL-034](tasks.md#LL-034) **V1 firmware pre-flash review + critical fixes (Apr 28).** Read through the existing 4 V1 modules (`state_bus`, `button`, `led_driver`, `pattern_interp`) plus supporting pieces (`nvs`, `provisioning`) before the Session 0 smoke flash. **One critical bug found** that would have panicked on first boot of the C3 dev board: `button.c::ll_button_init()` was unconditionally calling `gpio_isr_handler_add(LL_PIN_BUTTON_RESET, ...)` and `gpio_get_level(LL_PIN_BUTTON_RESET)`, but `c3_devkit.h` sets `LL_PIN_BUTTON_RESET = -1` because the V0 prototype board has no recessed button (header comment said "button module skips registration" but the code didn't actually skip — host tests didn't catch it because they exercise pure `button_logic.c`, not the IDF glue). Patched: all three `LL_PIN_BUTTON_RESET` operations in `ll_button_init` now guarded by `if (LL_PIN_BUTTON_RESET >= 0)`; init log message handles both cases; comment in code explains the contract. Compiler dead-code-eliminates the guarded blocks on c3_devkit since `(-1) >= 0` constant-folds to false — zero runtime cost. Also corrected `c3_devkit.h::LL_LED_COUNT_DEFAULT` 32 → 66 to match the actual V0 prototype hardware (the 12×12 test rig per `project_quarter_spring2026` memory; production 6×6 / 32-LED ships on `prod_v1_pro.h`, unchanged). Pin assignments verified against the ESPHome V0 YAML: GPIO 8 = LED data, GPIO 9 = primary button (active-low, internal pullup, inverted) — matches `c3_devkit.h`. Host tests don't reference either macro, so changes are isolated. Non-blocking observations also surfaced: no `sdkconfig.defaults` or `partitions.csv` files exist (IDF will auto-generate from chip defaults — fine for smoke flash where OTA is out of scope); two event loops by design (state_bus's custom loop + IDF default for esp_wifi); GPIO 9 is a strap pin (boot-mode select), so holding the button during reset puts the chip in download mode — handy fallback if `idf.py flash` ever fails to auto-reset.

- [x] [LL-035-0](tasks.md#LL-035-0) **Session 0 — V1 firmware first flash on real silicon (Apr 28).** Greenfield V1 firmware booted end-to-end on the XIAO ESP32-C3 prototype for the first time after two more pre-flash-review-missed bugs surfaced during boot and were patched on the spot. **Bug 1 (panic):** `pattern_interp.c::TASK_CORE` was `1` but the C3 is Unicore — `xTaskCreatePinnedToCore` asserted on `xCoreID < 1` mid-init (after `led_driver: init: pin=8 leds=32` and before `button_init`). Fixed by setting `TASK_CORE = 0` with a comment noting C3 + C6 are both single-core from FreeRTOS's perspective (C6's low-power core isn't scheduled). **Bug 2 (data):** `state_bus_defaults.c::ll_state_defaults` had `led_count = 32` hardcoded, which propagated through nvs.c's first-boot path and overrode the board header's `LL_LED_COUNT_DEFAULT=66`. Fixed to `66` with a TODO documenting the proper architecture (nvs.c should pull `LL_LED_COUNT_DEFAULT` from board.h after calling defaults; state_bus_defaults.c shouldn't carry a board-dependent value at all). After patching: incremental rebuild in 8s, reflash via `idf.py flash` (auto-reset over USB Serial/JTAG worked, no manual BOOT button needed). Boot log clean: `Unicore app`, app version `50035df` (matching commit), `nvs/state_bus/led_driver/pattern_interp` all init with `leds=66` end-to-end, `provisioning: no creds — radios dark`, `button: init: primary=GPIO9 (no recessed button on this board)` confirming the earlier `LL_PIN_BUTTON_RESET=-1` guard. Bench test over 30s captured 7 state-bus events: 5 single-press color cycles (#3214FF → #8000FF → #DF00FF → #FF00BF → #FF0060), then 2 double-press pattern advances (solid → rainbow → scanner). Bill confirmed visible LED response on the strip. Session 0 closes green; Session 1 (transport scaffold) is next. Build flagged: app binary is 983KB / 1MB partition (94% used) — adding transport + webapp + mDNS will require a custom `partitions.csv` with the `ab_with_factory` 4MB scheme that `c3_devkit.h` already names.

- [x] [LL-035](tasks.md#LL-035) **App demo mini-sprint locked (Apr 28).** Apr 22 parked app decisions resolved this session. Path A + B + C (transport + webapp + RN app) committed for an Apr 28 → May 5 push targeting an end-to-end faculty-advisor demo of both apps controlling a real ESP32 mirror. Stack locked: React Native (bare, Android only this sprint — iOS deferred, needs a Mac), Preact + Vite for webapp, `esp_http_server` for transport (built-in, handles both REST and WS from one handler), cJSON for envelopes. **Demo board = ESP32-C3 dev kit** (the existing V0 prototype hardware, already wired) — V0 ESPHome firmware will be overwritten by V1, recoverable via ESPHome device builder if needed. C6 + Matter demo deferred until post-mini-sprint. Demo network = Bill's phone hotspot (avoids university Wi-Fi captive portals + IT security). Auth = open mode only for the demo — HMAC stays specced and the wire envelope's `hmac` field stays optional, so this incurs zero protocol debt. **RN app skips BLE provisioning entirely for V1**: webapp captive portal handles all first-time pairing, RN app discovers an already-paired mirror via mDNS (`react-native-zeroconf`). Pattern editor confirmed OUT (V2). 5 demo capabilities: setup (webapp only), on/off, color, pattern select, factory reset. State sync over WS = server broadcasts `LL_EV_STATE_CHANGED` to all clients + clients re-`state.subscribe` on connect or app-foreground for snapshots. **Build flavor = stock unsigned dev builds** — no secure boot, no flash encryption, no anti-rollback (all eFuse burns, irreversible; production hardening is post-sprint). Hardware safety rules locked: no `espefuse.py burn_*`, no `CONFIG_SECURE_BOOT_V2_ENABLED`, no `CONFIG_FLASH_ENCRYPTION_ENABLED`, no `CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK` for the duration of the sprint. **Session 0 added** ahead of Session 1: smoke flash existing 4 V1 modules (state_bus, button, led_driver, pattern_interp) to the C3 dev board to validate toolchain + build/flash pipeline + button-LED end-to-end on real silicon before piling transport on top. 8-session plan, known risks (greenfield transport, RN setup tax, mDNS over hotspot flagged as primary danger, webapp ≤80KB bundle budget, reflash iteration speed), stretch goals (BLE provisioning in RN, UI polish), and demo-day Definition of Done all tracked in [docs/mini-sprint-app-demo.md](docs/mini-sprint-app-demo.md). RN dev environment confirmed working post-reboot (ANDROID_HOME / JAVA_HOME / PATH all resolve cleanly; AVD `Pixel_9_Pro_-_emulator` created and paired with physical Pixel 9 over wireless ADB). Sprint plan Week 5 updated to extend through May 5 and link the mini-sprint doc.

- [x] [LL-035-1-1](tasks.md#LL-035-1-1) **Session 1 foundation: 4MB ab_with_factory partition table verified on silicon (Apr 29).** Prerequisite for the rest of Session 1 — yesterday's Session 0 closed with the V1 binary at 983KB / 1MB (94% used), and Session 1 piles `esp_http_server` + cJSON + mDNS on top, so the default single-app partition would overflow before transport even compiled. Foundation laid before any transport code lands. Two new files at the firmware project root:
  - **[Firmware/v1/partitions.csv](Firmware/v1/partitions.csv)** — `ab_with_factory` scheme on 4MB flash (the layout `c3_devkit.h::LL_PARTITION_SCHEME` already names): `nvs` 24KB at 0x9000 (kept at the IDF default offset on purpose so existing NVS contents survive the partition swap), `otadata` 8KB at 0xf000, `factory` 1.25MB at 0x20000, `ota_0` 1.25MB at 0x160000, `ota_1` 1.25MB at 0x2a0000. Total used 3.875MB; top 128KB intentionally unallocated as room for a future coredump partition without reshuffling app slots.
  - **[Firmware/v1/sdkconfig.defaults](Firmware/v1/sdkconfig.defaults)** — minimal: 4MB flash size, custom partition table pointing at `partitions.csv`. Production-hardening flags (secure boot, flash encryption, anti-rollback) explicitly *commented* with a pointer to the mini-sprint hardware safety rules — they're eFuse burns, forbidden this sprint, and the comment makes that load-bearing.

  Verified end-to-end on the XIAO ESP32-C3 prototype:
  1. **Build green** (`idf.py -DLL_VARIANT=standard -DLL_BOARD=c3_devkit set-target esp32c3 build`) — gen_esp32part.py produced a partition table dump matching the CSV verbatim (5 rows, correct offsets/sizes); link reported `binary size 0xf0010 bytes. Smallest app partition is 0x140000 bytes. 0x4fff0 bytes (25%) free` — i.e. 983KB binary / 1.25MB slot, 320KB headroom for the rest of Session 1.
  2. **Flash green** (`idf.py -p COM3 flash`) — esptool wrote bootloader at 0x0, partition table at 0x8000, otadata at 0xf000, app at 0x20000 (the new factory offset), all four with hash verification.
  3. **Boot green** (12s pyserial capture after RTS-pulse reset) — bootloader read the new partition table from flash and dumped all 5 entries; "Defaulting to factory image" → "Loaded app from partition at offset 0x20000"; app version `37be211` (matches the Session 0 commit, no source change); all four core modules init cleanly in order, no panics. **NVS state from yesterday survived the partition swap** (`nvs: loaded: on=1 pattern=random color=#FF00BF brightness=25 leds=66`) — confirms the 0x9000 nvs offset choice was correct.

  No source code touched in this step — pure flash-layout + build-config foundation. The 25% free in the factory slot is the budget for transport + mDNS + webapp embed in the rest of Session 1; if it goes above ~85% utilization mid-sprint, raise per-slot size by stealing from the unused top region. `Firmware/v1/sdkconfig` is gitignored so each dev's machine regenerates it from `sdkconfig.defaults` on first build; existing Session 0 sdkconfigs need a `Remove-Item Firmware/v1/sdkconfig` followed by a clean build to pick up the partition flags. Worktree-root `*.log` files (build/flash/boot capture) added to `.gitignore` — narrative of each verification run lives here in sprint_log.md, raw logs aren't worth versioning.

- [x] [LL-035-1-2](tasks.md#LL-035-1-2) **Session 1 mini-milestone: `core/ll_mdns/` scaffolded + alive on silicon (Apr 29).** First of the two new components for Session 1. Module advertises the device on the local network so app/webapp clients can find it without manual IP entry — implements [control-protocol-spec §2.2](docs/control-protocol-spec.md). Wraps the espressif/mdns managed component (declared as a dependency in [Firmware/v1/core/ll_mdns/idf_component.yml](Firmware/v1/core/ll_mdns/idf_component.yml), fetched by the IDF Component Manager into `managed_components/`).
  - **Public surface** (in [ll_mdns.h](Firmware/v1/core/ll_mdns/ll_mdns.h)): two functions matching the existing init/subscribe pattern other modules use — `ll_mdns_init()` brings up mDNS + sets the hostname; `ll_mdns_subscribe()` wires `LL_EV_WIFI_CONNECTED` → publish, `LL_EV_WIFI_DISCONNECTED` → tear down, `LL_EV_STATE_CHANGED + which==AUTH_MODE` → republish only the auth TXT.
  - **Service published** (when wifi connects): `_layeredlogic._tcp.local` on port 80 with TXT records `variant=<standard|matter>`, `version=<fw>`, `id=<6-hex MAC suffix>`, `auth=<open|paired>`. Three of those four are fixed at boot (cached at init); only `auth` mutates at runtime so we use `mdns_service_txt_item_set` for that one rather than a full re-add.
  - **Hostname**: `layered-logic-mirror-XXXXXX.local` where XXXXXX is the lower 3 bytes of the WiFi STA MAC, lowercase hex. On the V0 prototype that resolves to `layered-logic-mirror-b2332c.local` (b2:33:2c is the actual silicon ID).
  - **Compile-time variant string**: top-level [Firmware/v1/CMakeLists.txt](Firmware/v1/CMakeLists.txt) now also injects `LL_VARIANT_NAME` as a quoted string macro alongside the existing `LL_BOARD_*` flag, so any source file can read the variant at runtime. mdns is the first consumer; future telemetry/OTA paths will share it.
  - **Component name lesson learned**: first build attempt failed with `Target "__idf_mdns" links to itself` because both my local component and `espressif/mdns` registered as `mdns` in the build graph. Fixed by renaming the folder `core/mdns/` → `core/ll_mdns/` (component name is derived from folder name; can't be overridden in `idf_component_register`). Header was already named `ll_mdns.h` for the same reason on the C side, but the folder rename completes the symmetry. Documented in [the CMakeLists comment](Firmware/v1/core/ll_mdns/CMakeLists.txt) so future contributors don't repeat it.
  - **Verified on silicon** (XIAO ESP32-C3, COM3): build green (binary 0xfad30 / 1.25MB slot, 22% free — mdns added ~42KB on top of the foundation 983KB); flash green (4 hash-verified writes); boot green (12s pyserial capture after RTS-pulse reset). Boot log confirmed (a) the espressif/mdns task announcing `mDNS task will be created from internal RAM`, (b) our `mdns: init: hostname=layered-logic-mirror-b2332c.local (service deferred until wifi up)` line, (c) correct boot ordering with mdns slotted between provisioning and led_driver, (d) zero regression in the four existing modules — NVS state from yesterday survived (`on=1 pattern=random color=#FF00BF brightness=25 leds=66`). The "service deferred" branch firing is the right behavior for the wifi-down state — won't advertise a record pointing at an IP we don't have. Service publication itself will get its first end-to-end test once wifi comes up via the captive portal in Session 4.

- [x] [LL-035-1-3](tasks.md#LL-035-1-3) **Session 1 milestone HIT: `core/transport/` + dev SoftAP + wscat ping/pong verified end-to-end on silicon (Apr 29).** The Session 1 Definition of Done from the mini-sprint plan was "transport reachable; `wscat` ping/pong works." All green. Three subsystems landed in this milestone:
  - **[core/transport/](Firmware/v1/core/transport/)** — wraps `esp_http_server`. Public surface is the now-familiar `ll_transport_init` / `ll_transport_subscribe` pair. Server lifecycle is wifi-gated: starts httpd on `LL_EV_WIFI_CONNECTED`, stops on disconnect (no point listening on a netif we don't have an IP on). WS handler at `/ws` parses the [control-protocol-spec.md](docs/control-protocol-spec.md) JSON envelope via cJSON, dispatches by `op` field. Session 1 implements only `ping` (returns `{fw_version, uptime_s}`); every other op returns `{ok:false, error:{code:"unknown_op"}}` to keep clients honest about which protocol version they're talking to. Open auth mode only — `hmac` field stays optional in the envelope. WS-related types are gated behind `CONFIG_HTTPD_WS_SUPPORT=y` which `sdkconfig.defaults` now sets (re-added after deferring it from the foundation commit).
  - **Dev SoftAP path in [core/provisioning/](Firmware/v1/core/provisioning/)** — gated by `#define LL_DEV_OPEN_SOFTAP 1` (default ON for Sessions 1-4, override to 0 to test the production "radios dark" boot path). When no Wi-Fi creds are saved, the device opens a plain SoftAP `LL-Mirror-XXXXXX` (open, no password, max 4 clients) at boot. Bypasses the `wifi_prov_mgr` protocomm dance entirely — Sessions 1-4 don't need it because we're testing functionality, not the demo network path. Session 5 captive portal flow replaces this with the real `wifi_prov_mgr` SoftAP scheme. New public function `ll_provisioning_kick_dev_softap()` is a no-op in production builds, safe to call unconditionally from main.c.
  - **Compile-time variant string + `CONFIG_HTTPD_WS_SUPPORT=y`** — top-level [Firmware/v1/CMakeLists.txt](Firmware/v1/CMakeLists.txt) injects `LL_VARIANT_NAME` (added in the previous mdns commit; documented here for completeness). `sdkconfig.defaults` adds the WS support flag — required for `httpd_ws_frame_t`, `HTTPD_WS_TYPE_TEXT`, and `httpd_ws_send_frame` to be defined. Without it, transport.c fails to compile.

  **Two bugs caught and fixed in-session because of the verify-each-change cadence:**

  1. **`sdkconfig` doesn't pick up `sdkconfig.defaults` once it exists.** First transport build failed with `unknown type name 'httpd_ws_frame_t'`. Diagnosis: the foundation build had already generated `sdkconfig` *without* `CONFIG_HTTPD_WS_SUPPORT=y` (because that line wasn't in defaults at foundation time), and IDF doesn't re-merge `sdkconfig.defaults` into an existing `sdkconfig`. Fix: `Remove-Item Firmware/v1/sdkconfig` then rebuild; defaults regenerate it from scratch. This is exactly the workaround we documented in the foundation commit — confirmed it works in practice. Bill's main worktree will need the same one-time delete when he flashes there.

  2. **AP fast path raced subscribe-order.** First `start_dev_softap` impl called `esp_wifi_start` synchronously inside `ll_provisioning_init`. AP comes up in <50ms (vs the real STA path's 5-30s connect), so `WIFI_EVENT_AP_START` fired, the synthesized `LL_EV_WIFI_CONNECTED` posted, and the state-bus loop dispatched it — all *before* mdns and transport had subscribed. Both modules silently missed the event, kept logging "deferred until wifi up" forever even though wifi was up. Fix: split `start_dev_softap` into `init_dev_softap` (config + handler register, no start) and a new public `ll_provisioning_kick_dev_softap` (just `esp_wifi_start`); main.c calls the kick at the end after every `*_subscribe` has run. Real STA path doesn't have this race (natural connect delay) so it stays in `ll_provisioning_init` unchanged. Lesson surfaced: the firmware's init phase uses `init` for setup-only and `subscribe`/`kick` for anything that posts events — synchronizing event-emission with handler-registration is now an explicit pattern, not implicit.

  **Verified on silicon** (XIAO ESP32-C3, COM3, post-fix):
  - **Build green**: binary 0xfe400 / 1.25MB slot, 21% free (264KB headroom). Transport+dev-SoftAP added ~13KB on top of mdns; `esp_http_server` and `cJSON` were already linked transitively, only the WS handler symbols + transport.c are new.
  - **Boot ordering correct**: provisioning configures the AP and logs `DEV SoftAP configured (...) — deferred until subscribers wired`; mdns/transport/led_driver/pattern_interp/button all init in order; THEN at I (604) `wifi:mode : softAP`, `DHCP server started ... 192.168.4.1`, `DEV SoftAP up: ssid="LL-Mirror-B2332D"`. Mdns receives the event and logs `published _layeredlogic._tcp.local:80 (variant=standard version=9874b74-dirty id=b2332c auth=open)` — service is on the air with all 4 TXT records. Transport receives it and logs `listening on :80 (ws /ws)` — esp_http_server up, /ws URI handler registered.
  - **End-to-end Session 1 Definition of Done validated from a real client**: Bill's PC sees `LL-Mirror-B2332D` in its wifi list, connects (open network), gets a 192.168.4.x DHCP lease; `wscat -c ws://192.168.4.1/ws` connects; sending `{"op":"ping","req_id":"t1","ts":...}` returns `{"op":"ping","req_id":"t1","ok":true,"result":{"fw_version":"9874b74-dirty","uptime_s":...},"error":null}`. Unknown ops correctly return `{ok:false, error:{code:"unknown_op"}}`. Transport reachable, app-layer ping/pong round-trips, JSON envelope contract works. Session 1 closed.

- [x] [LL-035-2-1](tasks.md#LL-035-2-1) **Session 2a mini-milestone: `get_state` op live (Apr 29).** First op of three for Session 2 (transport ↔ state_bus). Read-only, simplest, lowest risk. Two changes to [transport.c](Firmware/v1/core/transport/transport.c):
  - `state_to_json()` helper — serializes `ll_state_t` → cJSON object matching control-protocol-spec.md §5 (all 7 fields: `on`, `pattern_id`, `base_color`, `brightness`, `led_count`, `auth_mode`, `telemetry_enabled`). `base_color` over the wire is the uppercase hex string `"#RRGGBB"` (per spec) rather than the internal uint32 — small format conversion the helper owns.
  - `get_state` op handler — calls `state_to_json(ll_state_bus_get())` and attaches as `result`. State_bus exposes a `const ll_state_t *` directly so no copy/lock — the read races the writer task by design (eventual consistency; word-aligned reads are atomic on RISC-V).
  - **Verified end-to-end** (XIAO ESP32-C3 on COM3, Bill's PC over wifi to LL-Mirror-B2332D SoftAP): build green (0xfe580 / 1.25MB slot, 21% free, +384 bytes for the helper); boot regression-free; full round-trip `get_state` → response containing the canonical state object, all 7 fields populated, values match yesterday's persisted NVS (`on=true, pattern_id="random", base_color="#FF00BF", brightness=25, led_count=66, auth_mode="open", telemetry_enabled=false`); existing `ping` op still works alongside (dispatch fan-out didn't regress).
  - **Tooling note (parked, not blocking):** `wscat 6.1.0` on the dev machine swallows responses when invoked with `-x <message> -w <seconds>` for one-shot tests — connection succeeds, message sends, but nothing prints to stdout. Workaround: `pip install websockets` + a 10-line Python script that does `connect → send → recv → print` synchronously. Adding to dev tooling notes in case a future session hits the same. Interactive `wscat` (the way Bill used it for Session 1's manual test) still works fine — only the scripted/non-interactive form is broken.

- [x] [LL-035-2-2](tasks.md#LL-035-2-2) **Session 2b mini-milestone: `set_state` op live — device controllable from any WS client (Apr 29).** Second of three Session 2 mini-milestones. The pivotal one — this is what makes the WS endpoint a real control surface, not just a read-only data port. Two changes to [transport.c](Firmware/v1/core/transport/transport.c):
  - `parse_hex_color()` helper — accepts `"#RRGGBB"` or `"RRGGBB"`, validates length + hex chars, returns false on any malformation so callers reject quietly rather than commit a bad color.
  - `op_set_state()` — for each recognized field present in the payload, posts the matching event to the state-bus loop: `on` → `LL_EV_POWER_TOGGLE`, `base_color` → `LL_EV_BASE_COLOR`, `brightness` (with 0–100 bounds check) → `LL_EV_BRIGHTNESS`, `pattern_id` → `LL_EV_PATTERN_CHANGE`. State_bus task applies them sequentially; pattern_interp/nvs/led_driver react via their existing LL_EV_STATE_CHANGED subscriptions. Sensitive fields explicitly NOT accepted via set_state: `auth_mode` (use the dedicated `set_auth_mode` op — security action), `telemetry_enabled` (use `set_telemetry` — privacy opt-in), `led_count` (read-only).
  - **Design choice surfaced and documented:** the set_state response's `result` is **best-effort** — `ll_state_bus_post` is async (state_bus has its own task), so by the time the response is built on the httpd task, the changes may not have been applied yet. The response will likely show the pre-update state. This is intentional, not a bug: the spec says set_state's result is the "Updated state object" but enforcing that synchronously would require a yield-or-spinlock that's worse than the racy read. Rationale captured in the op_set_state doc comment. The fix is Session 2c's `LL_EV_STATE_CHANGED` broadcast, which IS authoritative — clients should ignore set_state's result and react to the broadcast that follows.
  - **Verified end-to-end** (XIAO ESP32-C3 on COM3, Python websockets driver from the dev PC over the SoftAP). Six-step test sequence all green: snapshot before; set color+brightness (response stale, as expected); snapshot after (changes landed); bad payload (string instead of object) returns `{ok:false, code:"bad_payload", message:"set_state requires an object payload"}`; set pattern_id; final snapshot showing all 4 fields mutated. Build green (0xfe890 / 1.25MB slot, 20% free, +784 bytes for the helper + handler + dispatch entry). **Visual confirmation from Bill: LEDs went bright green after set_state + brightness=100; then breathing/blue/50% on the follow-up demo.** Full stack proven: WS → JSON envelope → cJSON parse → state_bus event post → state_bus apply → pattern_interp render → led_driver output, all six layers wired and live. The Session 2 milestone definition ("device controllable from any WS client") is functionally satisfied by 2b alone; 2c upgrades this to *cross-client-sync* which is the demo-day requirement.

- [x] [LL-035-2-3](tasks.md#LL-035-2-3) **Session 2 milestone HIT: cross-client state-change broadcasts (Apr 29).** Closes Session 2's Definition of Done from the mini-sprint plan. The full webapp/RN/wscat triangle now stays in sync: any client can change state, every other client sees the new state within ~70ms. This is the foundation for the cross-client demo day requirement (webapp + phone open simultaneously, change from one, the other UI updates ≤200ms).
  - **Two surface additions to [transport.c](Firmware/v1/core/transport/transport.c):** a new `broadcast_state()` function and a new state-bus subscription. `broadcast_state` builds the `{op:"state", ts, state:{...}}` envelope per [control-protocol-spec §3.3](docs/control-protocol-spec.md), enumerates open sockets via `httpd_get_client_list`, filters to WebSocket clients via `httpd_ws_get_fd_info`, and pushes via `httpd_ws_send_frame_async`. Subscribed via `LL_EV_STATE_CHANGED` on the state-bus loop in `ll_transport_subscribe`. Failed sends per-client are logged and skipped — framework reaps dead sockets on its own; we don't manage a separate liveness list. **No-op when no server is up** (wifi-down state) — broadcast just early-returns.
  - **Verified end-to-end with two simultaneous WS clients** + a third controller client driving the writes:
    - **Single-field test** (`set_state {base_color: "#FF8000"}`): both watchers received exactly 1 broadcast each, ~65ms after the controller sent. Broadcast payload carried the new `#FF8000`; direct controller response carried the stale prior state — confirming the design contract that clients should treat broadcasts as authoritative.
    - **Multi-field test** (`set_state {pattern_id, base_color, brightness}`): each watcher received exactly 3 broadcasts in rapid succession. Interesting observation: all 3 broadcasts carry the **same final state**, not intermediate states. Reason: `set_state` posts 3 events FIFO to the state-bus queue; state_bus task processes them all (each applies + re-dispatches `LL_EV_STATE_CHANGED`); by the time the first re-dispatch fires the broadcast handler, all 3 events are already applied, so each subsequent broadcast reads the same current state. Idempotent for clients but mildly wasteful (3 frames worth ~700 bytes total when 1 would do). Coalescing is a future optimization — Session 2 minimum scope leaves it.
  - **`ts` field on broadcasts is currently uptime-since-boot, not Unix time** (spec §3.3 implies the latter). Device has no NTP sync yet; that lands when provisioning matures in Session 5+. Clients can use the field as a strictly-monotonic ordering token for now. Tracked as a documented gap, not a bug.
  - **Build green** (0xfeb50 / 1.25MB slot, 20% free, +704 bytes for the broadcast machinery — bringing Session 2's total cost to ~1.9KB). Boot regression-free; LL_EV_STATE_CHANGED subscription registers cleanly alongside the existing WIFI_CONNECTED/DISCONNECTED handlers. **Visual confirmation from Bill**: LEDs flashed orange (test 1) then transitioned to a rainbow pattern at 75% brightness (test 2), matching the wire-level state sequence.

- [x] [LL-035-3](tasks.md#LL-035-3) **Session 3 milestone HIT: webapp scaffold + first click changes mirror color (Apr 29).** The C → TS/JS pivot. Closes Session 3's DoD ("first webapp click changes mirror color") from the mini-sprint plan. End-to-end stack now spans browser → Vite dev server → loopback → wifi → SoftAP → ESP32 → state_bus → pattern_interp → led_driver, with the broadcast path from Session 2c bouncing the new state back to the UI for free. Today closes with three sessions banked (1 + 2 + 3) — half the mini-sprint done in a single day, plus the foundation work.
  - **Stack laid down at [Firmware/v1/webapp/](Firmware/v1/webapp/):** Vite 5.4 + Preact 10.22 + TypeScript 5.5 with `@preact/preset-vite`. Standalone npm project (own [package.json](Firmware/v1/webapp/package.json), no monorepo hoisting). Vite output renamed to `app.js` / `app.css` with `cssCodeSplit: false` so the eventual ESP32 embed has stable filenames to register with `esp_http_server`'s static handler — keeps Session 5's captive-portal embed step trivial. `tsconfig.json` strict mode on, `jsxImportSource: preact` so JSX transforms route to Preact's runtime instead of pulling React in by accident. `.gitignore` covers `node_modules/`, `dist/`, `.vite/`, `*.log`. Standard `src/vite-env.d.ts` reference dropped after the first `tsc -b` flagged `import.meta.env.DEV` as missing — quick gotcha worth noting for the RN port (RN's `Metro` doesn't have the same env shape).
  - **WS client at [src/ws-client.ts](Firmware/v1/webapp/src/ws-client.ts) is deliberately Preact-free** so Session 7 can port it to the RN app verbatim (along with [src/protocol.ts](Firmware/v1/webapp/src/protocol.ts) which carries the wire types — `DeviceState`, `RequestEnvelope`, `ResponseEnvelope`, `StateBroadcast`, `PatternId` union of the 7 builtin IDs). `MirrorClient` exposes `connect/disconnect/send/getState/setState/ping`, with response correlation via a `Map<req_id, {resolve, reject, timeout}>` and a 5s per-request timeout. Inbound frames are forked at `op === "state"` (broadcasts → `onState` callback) vs `req_id` lookup (responses → resolve the matching pending promise). `defaultDeviceUrl()` returns `ws://192.168.4.1/ws` in dev (`import.meta.env.DEV`) and `ws://${location.host}/ws` in prod — the same forward-compat pattern webapp-spec.md §5.3 calls out for the V2 hosted variant.
  - **UI at [src/app.tsx](Firmware/v1/webapp/src/app.tsx) + hand-rolled CSS at [src/styles.css](Firmware/v1/webapp/src/styles.css):** dark, monospace (Berkeley Mono per brand lock), italic-default body. Three panels: **connection** (endpoint + status badge with idle/connecting/open/closed color states), **color** (large swatch + hex display + native `<input type="color">` + 7-preset row using the button-interface §4.3 cycle colors), **state** (kv table reflecting all 7 fields of the canonical state object). Color sends use `onChange` not `onInput` so the picker doesn't flood the device while dragging — within the spec §7.1 10msg/sec rate limit. UI auto-refreshes from broadcasts via `onState` callback wired in `useMemo`/`useEffect`.
  - **Snapshot-on-open pattern:** when the connection transitions to `open`, the UI fires a `get_state` to populate the panels immediately rather than waiting for the next broadcast. Eliminates the "blank state until something changes" hole — important for the demo, since the device may be sitting idle for minutes between user actions. Session 4's other capabilities will follow the same pattern.
  - **Verified end-to-end** (XIAO ESP32-C3 on COM3 + dev PC on `LL-Mirror-B2332D` SoftAP, ethernet for internet so wifi can switch cleanly):
    - **Scaffold checkpoint:** `npm install` (85 packages, 8s), `npm run dev` started Vite on `127.0.0.1:5173` in 444ms; HTML + `/src/main.tsx` both 200; preview panel rendered the dark scaffold badge confirming Preact + JSX transform pipeline both work.
    - **Connection checkpoint:** Bill connected wifi to `LL-Mirror-B2332D`, reloaded preview. Status badge transitioned `idle → connecting → open` (green) within ~1s; "connection" panel showed `ws://192.168.4.1/ws` + `open`; "state" panel populated with the snapshot from NVS — confirmed *both* directions of the WS transport (request/response correlation via req_id and the snapshot path) plus the JSON envelope contract on the client side matching the firmware side from Sessions 1–2.
    - **DoD checkpoint:** color picker click and preset-button click both produced **instantaneous** LED color changes on the strip. State panel's `base_color` field updated to the new hex via the broadcast bouncing back. Bill's quote: "works great, change takes effect instantaneously."
  - **Production build green and well under budget:** `npm run build` produces `dist/index.html` 0.42KB / 0.26KB gzip, `dist/app.css` 2.19KB / 0.92KB gzip, `dist/app.js` 18.43KB / 7.41KB gzip — **total 8.6KB gzipped, ~11% of the 80KB Session 4 budget gate.** Most of the JS bytes are Preact itself (~10KB minified, ~4KB gzipped); my code adds ~3KB gzipped on top. Rolling into Session 4 with ~71KB of headroom for the remaining 4 controls (brightness slider, pattern dropdown, on/off toggle, factory reset) — far more than they'll need.

- [x] [LL-035-4](tasks.md#LL-035-4) **Session 4 milestone HIT: all 5 webapp capabilities wired + bundle budget gate green (Apr 29).** Closes Session 4's DoD ("All webapp capabilities functional") from the mini-sprint plan. Four sessions banked in a single day (1 + 2 + 3 + 4 + the foundation work). Webapp control surface is now feature-complete for the demo on the webapp side; remaining mini-sprint scope is captive portal (Session 5) + RN app (Sessions 6-8). All capabilities verified end-to-end against the live XIAO ESP32-C3 prototype on the dev SoftAP.
  - **Brightness slider** ([app.tsx](Firmware/v1/webapp/src/app.tsx)) — continuous 0-100 integer range, **commit-on-release** semantics. Initial pass tried live-during-drag with a 100ms throttle; the device dropped sockets after ~1s of dragging (esp_http_server's WS task can't sustain 10msg/sec inbound while *also* fanning broadcasts back out to N clients per tick — spec §7.1's 10msg/sec limit isn't actually enforced in transport.c yet, so there's no per-IP rate-limit policy doing the closing, just backpressure). Backed off to commit-on-release: `onInput` updates a local `dragBrightness` state so the thumb tracks the cursor smoothly during interaction, `onChange` (which fires on pointer release) sends exactly one `set_state` and clears the local override. Same UX language as the existing color picker. Bill explicitly asked for "higher fidelity than the 4 button stops" — the wire range is now full integer 0-100 (firmware bounds-check is `0 ≤ b ≤ 100`), discrete 25/50/75/100 stops were a button-modality-only constraint and don't apply to the digital surface.
  - **Pattern dropdown** ([app.tsx](Firmware/v1/webapp/src/app.tsx)) — native `<select>` over the 7 builtin pattern IDs (`solid` / `rainbow` / `scanner` / `spinner` / `random` / `breathing` / `twinkle`, source-of-truth in [src/protocol.ts](Firmware/v1/webapp/src/protocol.ts) as the `PatternId` union). One `set_state {pattern_id}` per change, broadcasts re-bind the selected option. Custom dropdown chevron is hand-rolled in CSS (no UI library, per the no-deps rule).
  - **On/off toggle = brightness 0 / restore-last-active** — Bill's framing, much cleaner than fighting the firmware's `LL_EV_POWER_TOGGLE` event-name red herring (turns out the state_bus handler is `g_state.on = p->on` — actually set semantics, not toggle, despite the name). The webapp tracks `lastActiveBrightness` via a `useEffect` that snapshots whenever `state.brightness > 0` lands from a broadcast (default 50 if never seen). Big toggle button at the top of the controls: `state.brightness > 0` → "ON" green dot + "tap to turn off" hint, click sends `{brightness: 0}`. `state.brightness == 0` → "OFF" grey dot + "tap to turn on", click sends `{brightness: lastActiveBrightness}`. Side effect verified live: dragging the brightness slider off zero auto-flips the toggle back to ON because the broadcast updates state.brightness, which the toggle derives `isOn` from.
  - **Factory reset = UI mockup, no wire op** — per scope call: the protocol spec doesn't currently have a `factory_reset` op (only ping/get_state/set_state/etc — full list in [control-protocol-spec §4](docs/control-protocol-spec.md)). `LL_EV_FACTORY_RESET` *exists* as a state-bus event (button.c posts it on the recessed-hold gesture, nvs.c handles by erasing the settings namespace, provisioning.c wipes Wi-Fi creds), so the firmware-side reset path is already there — only the new wire op + dispatch case in transport.c is missing. Deferred to later in the sprint per Bill's call ("we don't have many settings for a user to mess with or change yet, mostly clears patterns + account data + Wi-Fi creds"); Session 5 is a natural slot since it touches provisioning anyway. **Stub behavior:** "danger zone" panel at the bottom (red-tinted border, separated from regular controls), red-outlined "factory reset" button → native `window.confirm()` warning → on accept, shows a dashed-border inline notice "factory reset stub — wire op lands later in sprint". No network call, no state change. Looks demo-able, behaves harmless.
  - **WS client gained reconnect-with-backoff** ([ws-client.ts](Firmware/v1/webapp/src/ws-client.ts)) — the brightness slider work surfaced that the original Session 3 client had no reconnect logic, so any transient socket drop was permanent until manual reload. `MirrorClient.connect` now sets `wantOpen=true`; on `close` events, scheduled reconnect with exponential backoff (1s → 2s → 4s → 8s capped). `disconnect()` clears `wantOpen` so cleanup is honest. `autoReconnect: false` opt-out for tests. **Important caveat for HMR-driven dev:** because the `MirrorClient` instance is held in `useMemo([])`, HMR doesn't replace the client when ws-client.ts changes — only a hard reload does. Documented inline; the same constraint will apply when the RN app reuses this client in Session 7 (Metro Fast Refresh has the same trap).
  - **Local-state-during-drag pattern** generalizes beyond brightness — slider's controlled `value` would otherwise snap back to the device's reported value mid-drag every time a broadcast arrives, fighting the user. `dragBrightness` (null when idle, number while interacting) overrides the bound value during interaction; cleared in `onChange`'s release handler. Pattern documented in code; the RN app's slider will need the same shape.
  - **Verified end-to-end on silicon and at the wire** (XIAO ESP32-C3 on COM3 + dev PC on `LL-Mirror-B2332D` SoftAP, all four capabilities visually confirmed by Bill at the bench): brightness slider drag-and-release dims/brightens the strip across arbitrary integer values (37, 82, etc — proving "higher fidelity than 4 stops"); pattern dropdown swap from `random` → `breathing` → others all kicked the strip into the new pattern within a frame, dropdown stayed selected on the new value via the broadcast bouncing back; on/off toggle blanks the strip and restores it to the prior brightness on each click; factory-reset confirm dialog displays the warning text, cancel does nothing, accept shows the stub notice without touching state.
  - **Bundle budget gate: PASS.** `npm run build` → `dist/index.html` 0.42KB / 0.26KB gzip, `dist/app.css` 4.59KB / 1.44KB gzip, `dist/app.js` 21.00KB / 8.17KB gzip — **total 9.87KB gzipped, ~12% of the 80KB Session 4 budget gate.** Session 4 added ~1.3KB gzipped on top of Session 3's 8.6KB baseline. ~70KB headroom remaining for Session 5 (captive portal HTML form + provisioning client logic) + future polish (settings panel, telemetry/auth surfaces, OTA UI).
  - **Layout (top-to-bottom):** connection → [power | pattern] → color → brightness → state → danger zone. Power button + pattern dropdown share a 2-column grid panel ("controls") since both are roughly button-sized and read better paired than as full-width siblings — Bill's call after seeing the first pass. Single-page, no router. Hash-based routing per webapp-spec §3.2 will land when there's actually >1 screen — Session 5 will probably split out a setup/captive-portal route, at which point the router lands.
  - **Parked / known issue: transient `socket closed` errors under user load.** Even with brightness on commit-on-release, occasional clicks (especially rapid sequences across multiple controls) flash a "socket closed" error in the connection panel. The reconnect-with-backoff recovers within 1-8s and the demo stays functional, but the error message is visible long enough to be noticed. Hypothesized root cause: `esp_http_server`'s WS handler is single-task and serializes both inbound dispatch *and* outbound broadcasts; any state change triggers a fanout to all connected clients, so a single inbound `set_state` produces N+1 frames worth of work on that one task. Add network jitter on the SoftAP and the framework's send queue can fill, dropping the connection. Possible fixes (none are this sprint, all post-demo): (a) move broadcast fanout to a separate task / queue so inbound dispatch doesn't block, (b) coalesce broadcasts when `set_state` carries multiple fields (already noted as TODO in Session 2c), (c) implement the spec §7.1 rate limit so the device proactively rejects flood traffic before backpressure builds, (d) investigate whether the SoftAP power management is throttling. Webapp side already does the right things (commit-on-release, reconnect, no flood). Bill's demo-day call: ship it, document it. **Demo workaround if it bites during the May 5 advisor demo:** reload the page once at the start to ensure a fresh socket, avoid rapid-fire clicks across controls.

- [x] [LL-035-5-1](tasks.md#LL-035-5-1) **Session 5 mini-milestone: webapp bundle embedded in firmware + served from device flash (Apr 29).** First of five sub-milestones in Session 5 (the captive-portal session). Replaces the Session 3-4 dev-server workflow (`npm run dev` on `127.0.0.1:5173` connecting to `ws://192.168.4.1/ws`) with the actual demo-day topology: the device serves both the page and the WS endpoint from the same origin. Last piece needed for `http://192.168.4.1/` (and `http://layered-logic-mirror-b2332c.local/`) to be a real URL, not a development scaffold.
  - **New component [core/webapp_assets/](Firmware/v1/core/webapp_assets/)** — owns the embed and the static URI handlers. Public surface is one function: `ll_webapp_assets_register(httpd_handle_t)` ([webapp_assets.h](Firmware/v1/core/webapp_assets/webapp_assets.h)). Embeds the gzipped Vite outputs via `EMBED_FILES` and serves them at `/`, `/app.js`, `/app.css` with `Content-Type: <html|js|css>; charset=utf-8` + `Content-Encoding: gzip` + `Cache-Control: no-cache` (the no-cache is defensive for now — webapp ships inside firmware so OTA is the only mutation path; tighter caching is a post-demo polish item). Linker symbols (`_binary_index_html_gz_start` / `_end` etc.) are referenced via the `extern const uint8_t name[] asm("_binary_...")` form to keep the C reads symbol-name-correct without depending on toolchain-specific name mangling.
  - **Pre-gzip in npm build, not in CMake.** Added [Firmware/v1/webapp/scripts/gzip-dist.mjs](Firmware/v1/webapp/scripts/gzip-dist.mjs): a 20-line Node script that uses the stdlib `zlib.gzipSync` at `Z_BEST_COMPRESSION` to produce `dist/{index.html,app.js,app.css}.gz` after `vite build`. Wired into [package.json](Firmware/v1/webapp/package.json)'s `build` script (`tsc -b && vite build && node scripts/gzip-dist.mjs`). Cleaner than gzipping in CMake — keeps the firmware build purely C/asm, no shell-out to Python or Node from CMake, and the gzip ratios are reported by the script in dev for visibility (index.html 63%, app.js 39%, app.css 31% — total 9909 bytes embedded). The component CMakeLists checks for the `.gz` files at configure time and emits a friendly fatal error pointing the dev at `npm run build` if any are missing, instead of letting `EMBED_FILES` fail with the raw IDF error.
  - **Transport.c integration is two lines** ([core/transport/transport.c](Firmware/v1/core/transport/transport.c)): `#include "webapp_assets.h"` and a call to `ll_webapp_assets_register(g_server)` inside `start_server()` after `httpd_register_uri_handler` for the `/ws` route. Plus `webapp_assets` added to transport's `REQUIRES`. Also bumped `cfg.max_uri_handlers = 4 → 8` since we went from 1 URI (`/ws`) to 4 (`/ws` + 3 webapp assets), with headroom for the captive-portal probe URIs that land in sub-milestone 3 (Android `connectivitycheck.gstatic.com`, iOS `captive.apple.com`, etc.).
  - **One bug caught and fixed in-session because of the verify-each-change cadence.** First firmware build failed with `error: "/*" within comment [-Werror=comment]` — my docstring in `webapp_assets.h` had `dist/*.gz` and `dist/*` as path globs inside an outer `/* ... */` block, and `-Werror=comment` (default in IDF) treats nested `/*` as a footgun. Fixed by rewording the comment to use prose ("the .gz files under dist/", "each dist file into a .gz sibling") instead of glob patterns. Lesson: when documenting paths inside C comments, avoid `*` adjacent to a `/`. Also moved the boot-capture helper to its proper home: [Firmware/v1/scripts/boot_capture.py](Firmware/v1/scripts/boot_capture.py) (was at worktree root in this session — wrong place for a firmware-verification tool).
  - **Verified end-to-end on silicon** (XIAO ESP32-C3 on COM3, dev PC on `LL-Mirror-B2332D` SoftAP, ethernet up so wifi can switch cleanly):
    - **Build green**: 0x101590 / 0x140000 bytes, 20% free (251KB headroom). Bundle embed cost ~9.6KB on top of Session 4's 0xfeb50 baseline (matches the 9909-byte embed exactly, the rest is the 3 URI handler stubs + `serve_asset` helper).
    - **Boot green** (12s pyserial capture after RTS-pulse reset, [Firmware/v1/scripts/boot_capture.py](Firmware/v1/scripts/boot_capture.py)): app version `93f686c-dirty` matches working tree; all 8 modules init in order; new `webapp_assets: registered /  /app.js  /app.css  (9909 bytes gzipped total)` line slots cleanly between mdns publish and transport listen; existing modules behave unchanged. NVS state from prior session survived (`on=1 pattern=random color=#0000FF brightness=44 leds=66`) — the partition table didn't shift.
    - **Wire-level probe from dev PC** (`Invoke-WebRequest` over the SoftAP, IP `192.168.4.2 ↔ 192.168.4.1`): all three assets return `200`, correct `Content-Type`, `Content-Encoding: gzip`, decompressed sizes byte-exact match the Vite outputs (HTML 431B, JS 21062B, CSS 4682B). HTML body shipped verbatim with `<script src="/app.js">` and `<link href="/app.css">` references that the browser then re-fetches and gets served from the same handlers. WS at `/ws` still responds to ping alongside the new static handlers (`fw_version: 93f686c-dirty`, uptime ~3min) — confirms /ws and the static handlers don't fight over httpd's URI table.
    - **Visual confirmation from Bill**: `http://192.168.4.1/` and `http://127.0.0.1:5173/` (the still-running Vite dev server) behave **identically** in the browser — same UI, same controls, LEDs respond to clicks the same way. Embedded version is feature-equivalent to the dev server, with the bonus of being same-origin (no cross-origin gymnastics on the WS URL: `defaultDeviceUrl()` falls into its prod branch and returns `ws://${location.host}/ws`).
  - **Bonus side-quest: mDNS-over-SoftAP works on Windows.** While testing, ran `Resolve-DnsName layered-logic-mirror-b2332c.local` from the dev PC over the SoftAP and got an `A 192.168.4.1` answer in 120ms; `Invoke-WebRequest http://layered-logic-mirror-b2332c.local/` returned 200 with the embedded webapp. So the mDNS publish side from Session 1 is working correctly from a fresh client — useful baseline for the upcoming hotspot spike (sub-milestone 2). If hostname resolution fails over Bill's hotspot but works over the SoftAP, the failure mode is in the hotspot's multicast forwarding, not in our firmware. **The "better URL" Bill flagged in chat** (the captive-portal session URL story): this Session 5 already brings two real options into play — `http://192.168.4.1/` (gateway IP, always works during pairing) and `http://layered-logic-mirror-b2332c.local/` (mDNS hostname, the demo-day URL once the device is on the hotspot). Captive-portal hijack (sub-milestone 3) eliminates needing to type *any* URL during first-time pairing — the phone OS pops the page automatically.
  - **Sub-milestone 1 of 5 closed.** Next checkpoint: mDNS-over-hotspot spike (sub-milestone 2, ~30 min, hardware-network test on Bill's phone hotspot — Tier 1 risk per [mini-sprint-app-demo §3](docs/mini-sprint-app-demo.md)). After that: captive-portal DNS hijack, /setup screen + provisioning op, retire `LL_DEV_OPEN_SOFTAP=1`.

- [x] [LL-035-5-2](tasks.md#LL-035-5-2) **Session 5 sub-2 spike: mDNS-over-hotspot Plan A confirmed for the webapp demo path (Apr 29).** Tier 1 risk per [mini-sprint-app-demo §3 #3](docs/mini-sprint-app-demo.md) — *"Some Android hotspot implementations don't forward mDNS / Bonjour multicast cleanly."* This sub-milestone's only deliverable is a yes/no on whether the device is reachable via `http://layered-logic-mirror-XXXXXX.local/` from a webapp client when both the device and the client are on Bill's Pixel 9 hotspot. **Yes for PC + browser, deferred for phone-side until Sessions 6-8 (RN app exists by then).** No code committed from this spike — the test path was a throwaway dev-only block in `provisioning.c` that hardcoded creds + applied them with `WIFI_STORAGE_RAM` so they never touched NVS, then reverted before commit so neither the SSID nor the password ever entered version control.
  - **Test setup**: dev board on COM3 with the sub-1 firmware; Pixel 9 hotspot on; dev PC ethernet for internet so wifi could switch cleanly between SoftAP / hotspot. Hardcoded-creds block lived under `LL_DEV_HOTSPOT_CREDS` (default 1 for the spike), short-circuiting the `LL_DEV_OPEN_SOFTAP` path: `esp_wifi_set_storage(WIFI_STORAGE_RAM) → esp_wifi_set_mode(WIFI_MODE_STA) → esp_wifi_set_config + esp_wifi_start`. Existing event handlers (STA_START → connect, IP_GOT_IP → post LL_EV_WIFI_CONNECTED) carried the rest unmodified — the spike was deliberately the smallest possible delta against the production code path.
  - **Two surprises caught fast and fixed:**
    1. **SSID typo from chat.** Bill typed "Bils pixel 9" (single S) when sharing creds — actual hotspot SSID is "Bills pixel 9" (double S, matching the apostrophe-less possessive). First boot capture failed with `STA_DISCONNECTED reason=201 (NO_AP_FOUND)` × 10 retries over 27s. PC-side `netsh wlan show networks` revealed the correct SSID. Fixed with a one-character source edit + reflash.
    2. **Windows scan was lying about band availability.** First instinct after NO_AP_FOUND was "ESP32-C3 is 2.4GHz-only, hotspot must be 5GHz-only" — and `netsh wlan show networks mode=bssid` reinforced it (`SSID 8: Bills pixel 9, BSSID 1: 0e:b5:52:29:29:0d, Radio: 802.11ax, Band: 5 GHz, Channel: 48`). Bill pushed back: hotspot is configured dual-band, no 2.4-only option in Pixel settings. Reflashed the typo-fixed build to test directly. **The C3 found a *different* BSSID for the same SSID on 2.4GHz** — `connected with Bills pixel 9, aid = 2, channel 11, BW20, bssid = 0e:4a:ad:29:29:0d`. Conclusion: the Pixel 9 hotspot DOES broadcast on both bands, but Windows' aggregated scan view only showed the 5GHz BSSID at the moment of scan (some Pixel hotspots advertise the 2.4GHz radio more sparsely until a 2.4GHz client probes for it). **Lesson for the future: the C3's own scan is the authoritative "is 2.4GHz reachable" probe, not Windows.**
  - **Boot capture once SSID was right** (12s window after RTS reset, [Firmware/v1/scripts/boot_capture.py](Firmware/v1/scripts/boot_capture.py)): two short STA_DISCONNECTED retries (reasons 203, 205 — auth-stage transient handshake quirks, well within the 10-retry budget), then connect on retry 3 in ~6s, IP `10.160.147.61`, mdns publish, transport listening, webapp_assets registered. RSSI -37 (very strong). Total cold-boot-to-listening time: ~6.9s on the hotspot path vs ~0.7s on the SoftAP fast path — the difference is the AP probe + 4-way WPA2 handshake, normal for a real STA path.
  - **mDNS resolution from the dev PC over the hotspot** — five client tools tested, four passed:

    | Tool | What it does | Result |
    |---|---|---|
    | `Resolve-DnsName layered-logic-mirror-b2332c.local` | Native PS resolver (LLMNR + mDNS + DNS) | ✓ → `A 10.160.147.61` (matches the device's hotspot IP) |
    | `ping layered-logic-mirror-b2332c.local` | Win32 GetAddrInfo path | ✓ → resolves, replies in 30-54ms |
    | `curl.exe http://...local/` | WinINET/Schannel path | ✓ → HTTP 200 |
    | `python -c "urllib.request..."` | Python's `socket.getaddrinfo` | ✓ → HTTP 200, correct Content-Type |
    | `Invoke-WebRequest http://...local/` | .NET HttpClient | ✗ → "remote name could not be resolved" |

    The single failure is a **PowerShell-specific .NET HttpClient quirk** (known issue: some Windows builds don't wire `.local` mDNS into the .NET resolver path even when LLMNR/mDNS work everywhere else). Doesn't affect the demo — **browsers (Chrome, Edge, Firefox, Safari) use the Win32 `getaddrinfo` resolver path that ping/curl use, which works**. Worth knowing for future sessions: when scripting Windows-side HTTP probes against the device, prefer `curl.exe` or Python over `Invoke-WebRequest`.
  - **End-to-end browser confirmation by Bill**: opened `http://layered-logic-mirror-b2332c.local/` in the dev PC's browser over the hotspot, embedded webapp loaded identically to the dev-server experience, all controls (color picker, brightness slider, pattern dropdown, on/off toggle) drove the LEDs in real time. **This is the actual May 5 demo topology** — phone hosting hotspot, mirror as STA client, laptop as STA client browsing to the mirror via mDNS hostname. Plan A is alive for the webapp half of the demo.
  - **Phone-side mDNS over hotspot**: untested in this spike. Bill's Pixel 9 is the hotspot HOST, so it can't be a hotspot client in the same network — the test would need a second phone we don't have today. The relevant phone-side question is for Sessions 6-8 (`react-native-zeroconf` from the RN app) which lives on the same phone hosting the hotspot — Android lets the host phone reach hotspot clients via the AP interface, but multicast forwarding behavior there is exactly the open question the [mini-sprint plan §3 #3](docs/mini-sprint-app-demo.md) flagged. Plan B (hardcoded-IP fallback in the RN app) stays in the back pocket as the escape hatch if Session 7 hits this wall.
  - **Sub-2 closed; revert clean.** `git checkout -- Firmware/v1/core/provisioning/provisioning.c` reverted the spike block; tree confirmed clean (`git status` empty, `grep -rn "Bills pixel\|LayeredLogicTest" Firmware/` returns only build artifacts which are gitignored). Device is currently still on the hotspot from the last flash; that's fine — sub-3 will reflash with the SoftAP path back in default mode for the captive-portal work. Implication for the design forward: we can rely on the mDNS hostname for the embedded-webapp demo path without engineering a Plan B. Sub-3's captive portal is now purely a UX win (zero-click setup landing page) rather than a fallback for unreachable mDNS.

- [x] [LL-035-5-3](tasks.md#LL-035-5-3) **Session 5 sub-3: captive-portal DNS hijack + HTTP 404 redirect on silicon (Apr 29).** Third of five Session 5 sub-milestones. The flow that makes "join the SoftAP, your phone auto-pops the webapp" actually work — no manual URL typing, no IP memorization. Two new pieces, one minor extension, one weird bug.
  - **New component [core/captive_dns/](Firmware/v1/core/captive_dns/)** — UDP DNS responder, ~250 lines including comments. Public surface is the now-familiar `ll_captive_dns_init` / `ll_captive_dns_subscribe` pair. Lifecycle gated on AP mode: subscribes to `LL_EV_WIFI_CONNECTED` on the state-bus loop, calls `esp_wifi_get_mode()` at handler entry, and only spawns the responder task if mode is `WIFI_MODE_AP` or `APSTA`. On `LL_EV_WIFI_DISCONNECTED` (or any transition out of AP) it shuts the socket down, which wakes the recvfrom loop with errno set; the task self-terminates after closing its FD so we don't leak. Bind is `INADDR_ANY:53` since only the AP netif is up when this runs — no per-interface socket gymnastics. Wire format follows RFC 1035 §4.1 exactly: 12-byte header, echo the question, append a single 16-byte A answer (`0xC00C` name compression pointer + TYPE A + CLASS IN + TTL=60s + 4-byte RDATA = `192.168.4.1`). Every A query gets the same answer regardless of QNAME — that's the whole point of a hijack. AAAA / SRV / TXT queries also get an A answer; clients will fail-fast and retry as A, which is fine for captive-portal where we just need *one* successful resolution to route the OS to our HTTP server.
  - **HTTP 404 wildcard redirect** in [core/webapp_assets/](Firmware/v1/core/webapp_assets/webapp_assets.c) via `httpd_register_err_handler(server, HTTPD_404_NOT_FOUND, cap_404_redirect)`. Catches every URI we don't already serve: Android's `connectivitycheck.gstatic.com/generate_204`, iOS's `captive.apple.com/hotspot-detect.html`, Windows' `msftconnecttest.com/connecttest.txt`, etc. — they all fail to match `/`, `/app.js`, `/app.css`, or `/ws` and fall through to the err handler, which returns `302 Found` with `Location: /` and `Cache-Control: no-store`. The relative root (not a full URL) is deliberate: the OS probe was made with the captive domain in the Host header, the browser auto-completes against that host (resolved to 192.168.4.1 by our DNS hijack), and the captive sheet UX stays consistent across phone OSes. **STA-mode caveat documented inline:** esp_http_server doesn't support per-mode URI tables, so the err handler is global; if someone hits an unknown path on the device when it's a real station, they'll be redirected to / — a reasonable affordance for a typo'd URL, not a bug.
  - **Bumped `cfg.max_uri_handlers` 4 → 8** in [transport.c](Firmware/v1/core/transport/transport.c) (already done in sub-1) provides headroom; the err handler is registered separately from the URI table so this didn't actually consume a slot, but the headroom is still useful for sub-4's potential `set_wifi_creds` op.
  - **One weird bug debugged via incremental probe logs.** First boot of the new build: handler ran, `xTaskCreate` returned `pdPASS`, task entered, `socket()` succeeded with fd 57, `bind()` succeeded — and then the `ESP_LOGI("listening on udp :%d, hijacking all A queries → %u.%u.%u.%u", ...)` log line **silently never printed**, breaking the trace right where it should have confirmed the responder was up. Same symptom on the webapp_assets sub-1 log (`+ 404→/ captive redirect`). Common factor: a Unicode `→` (U+2192, RIGHTWARDS ARROW, 3-byte UTF-8 `E2 86 92`) inside a runtime format string. Replacing the arrows with ASCII (` to ` and ` 404 captive redirect`) made both log lines print immediately. Mechanism still not fully understood — em-dashes (`—`) in other format strings throughout the codebase work fine, including elsewhere in this same component (e.g. the `"not in AP mode"` log) — but the arrows specifically broke vsnprintf-style emission. **Defensive rule for future sessions: keep runtime format strings ASCII-only.** Comments are preprocessor-stripped, so they can keep their UTF-8 typography unchanged.
  - **Verified end-to-end on silicon and at the wire** (XIAO ESP32-C3 on COM3, dev PC on `LL-Mirror-B2332D` SoftAP, IP `192.168.4.2 ↔ 192.168.4.1`):
    - **Build green**: 0x101f00 / 0x140000 bytes, 19% free (-1% vs sub-1's 20% headroom). Sub-3 added ~2.4KB on top of sub-1's 0x101590 — the DNS responder + 404 handler + the `state_bus → captive_dns` event subscription.
    - **Boot green** (5s pyserial capture after RTS-pulse reset, [Firmware/v1/scripts/boot_capture.py](Firmware/v1/scripts/boot_capture.py)): `webapp_assets: registered /  /app.js  /app.css  + 404 captive redirect (9909 bytes gzipped total)`, `transport: listening on :80 (ws /ws)`, `captive_dns: listening on udp :53, hijacking all A queries to 192.168.4.1`, `mdns: published _layeredlogic._tcp.local:80`. NVS state survived across the partition-untouched reflash; existing button + LED + state-bus modules behave unchanged.
    - **DNS hijack probe** (`Resolve-DnsName -Server 192.168.4.1`, forcing the query through the device): `anything.example → 192.168.4.1`, `connectivitycheck.gstatic.com → 192.168.4.1`. Every A query, regardless of QNAME, returns the SoftAP gateway. TTL 60s, response built in O(question_size) bytes — on the order of a few hundred microseconds.
    - **HTTP wildcard redirect probe** (`curl.exe -H "Host: <captive-portal-domain>" "http://192.168.4.1/<probe-path>"`): Windows probe path `/connecttest.txt` → `302 Location: /`. Android probe `/generate_204` with Host `connectivitycheck.gstatic.com` → `302 Location: /`. iOS probe `/hotspot-detect.html` with Host `captive.apple.com` → `302 Location: /`. All three OS probe shapes routed through our 404 handler correctly. Direct hit on `/` still returns the embedded webapp at 200 (regression-free).
  - **What this means for first-time pairing UX**: a phone joins the SoftAP → DHCP gives it `192.168.4.1` as its DNS server (lwip's default for `esp_netif_create_default_wifi_ap`) → the OS issues its captive-portal probe → our DNS hijack resolves the probe domain to `192.168.4.1` → our HTTP server returns 302 to `/` → the OS sees the response signature doesn't match the expected probe success → captive-portal sheet pops automatically and lands the user on the webapp. **Zero typing, zero URLs to remember.** The two known limitations (documented in [docs/mini-sprint-app-demo.md](docs/mini-sprint-app-demo.md) section to update separately): (a) modern iOS / ChromeOS use HTTPS captive-portal probes that we can't intercept without per-device TLS certs, but they fall back to the older HTTP probe after ~5-10s so the sheet still appears with that delay; (b) some phones cache "no captive portal here" per-SSID, so a re-test may need "forget network" first.
  - **Sub-3 closed; sub-milestones banked today: 3 of 5.** Next: [setup screen + provisioning wire op] (sub-4) — bigger architectural decision (transport-side `set_wifi_creds` op vs `wifi_prov_mgr`'s built-in protocomm endpoint) where being fresh matters most. After that: retire `LL_DEV_OPEN_SOFTAP=1` (sub-5) and the dev shortcut goes with it.

- [x] [LL-035-5-4](tasks.md#LL-035-5-4) **Session 5 sub-4 HIT: setup screen + `set_wifi_creds` wire op + bug-fix sweep — webapp end-to-end demo flow works (Apr 29).** Closes the Session 5 Definition of Done from [mini-sprint-app-demo.md §2](docs/mini-sprint-app-demo.md): *"Webapp end-to-end demo works (captive portal + Wi-Fi entry + fall-through to home Wi-Fi)."* Real Pixel-9 hotspot creds verified on silicon — webapp submits creds → device leaves SoftAP → joins hotspot → mDNS republishes on the new netif → laptop switches networks, opens `http://layered-logic-mirror-b2332c.local/`, lands on the control page over the new netif, all 5 capabilities work. Garbage creds also verified — 15s fallback timer wipes the bad creds and restarts the SoftAP automatically, so a typo'd password no longer strands the device. The single biggest sub-milestone of Session 5; touches firmware (3 new code paths), webapp (full architectural refactor), protocol spec (state object extension), and bug log (1 closed, 2 opened).

  **Architectural decision: option B not C.** Sub-4 evaluated three paths for cred submission: (A) raw `esp_wifi_set_config` in transport.c, (B) JSON-WS `set_wifi_creds` op delegating into the same `esp_wifi`/NVS machinery `wifi_prov_mgr` uses internally, (C) full protocomm-HTTP via `wifi_prov_mgr`'s built-in endpoint with hand-rolled or bundled protobuf in the webapp. **Picked B**: webapp speaks the same JSON envelope as the rest of the protocol; device-side cred path uses standard `esp_wifi_set_config` + persistence + connect, identical bottom-half to what `wifi_prov_mgr_configure_sta` would do. Production HA / Google Home support is a *separate concern* — those go through the matter variant (already scaffolded at [Firmware/v1/variants/matter/](Firmware/v1/variants/matter/)) using Matter commissioning (BLE + setup code), not our SoftAP path. Future BLE provisioning support adds `wifi_prov_scheme_ble` alongside our existing flow without rewriting anything user-facing.

  - **Firmware additions** ([core/transport/transport.c](Firmware/v1/core/transport/transport.c) + [core/provisioning/provisioning.c](Firmware/v1/core/provisioning/provisioning.c) + [core/state_bus/state_bus_events.h](Firmware/v1/core/state_bus/state_bus_events.h)):
    - **`set_wifi_creds` op** (transport.c, ~50 lines): validates `payload.ssid` (1-32 chars per IEEE 802.11) and `payload.password` (empty for open networks, or 8-64 chars per WPA2 spec), then posts a new `LL_EV_WIFI_APPLY_CREDS` event via the state-bus loop. Returns `{ok:true, result:{applied:true, ssid}}` synchronously. The async event-post pattern is critical — it lets the JSON response flush over the SoftAP before the SoftAP itself goes away.
    - **`factory_reset` op** (transport.c, ~10 lines): replaces the UI-mockup stub from sub-1 with a real wire op. Posts `LL_EV_FACTORY_RESET` via `ll_state_bus_post`; existing downstream handlers do the work (state_bus → reset settings to defaults; nvs → erase namespace; provisioning → wipe wifi creds via `esp_wifi_restore`). LED flash confirmation cue from pattern_interp's existing factory-reset handling renders unchanged.
    - **State extension**: `state_to_json()` now adds `provisioning_active` (derived from `esp_wifi_get_mode() == AP/APSTA`) and `wifi_ssid` (current STA SSID, null when not on STA). Webapp uses these to auto-route between `/setup` (when in AP mode) and `/control` (when on real Wi-Fi). transport.c subscribes to `LL_EV_WIFI_CONNECTED`/`DISCONNECTED` and rebroadcasts state on transitions, so any connected client sees the wifi-related changes live. Added forward declaration of `broadcast_state` to handle the new call site.
    - **`on_wifi_apply_creds` handler** (provisioning.c, ~50 lines): runs on the state-bus task (not the httpd task). Begins with a deliberate `vTaskDelay(250)` so the JSON response has time to flush — without it, lwip's TCP queue gets dropped when `esp_wifi_stop` tears down the AP netif and the webapp's submit promise hangs. Then: `esp_wifi_set_storage(FLASH)` → `esp_wifi_stop` → `esp_wifi_set_mode(STA)` → `esp_wifi_set_config` → `esp_wifi_start`. The existing `WIFI_EVENT_STA_START` → `esp_wifi_connect` → `IP_EVENT_STA_GOT_IP` → `LL_EV_WIFI_CONNECTED` chain takes it from there.
    - **15s fallback timer** (provisioning.c, ~60 lines): the demo-saving piece. Without this, a typo'd SSID or wrong password leaves the device stuck in failing-STA mode forever — the existing 10-retry loop spins indefinitely after the budget exhausts, recoverable only by recessed-button factory reset (which doesn't exist on the C3 dev board). With it: arm `g_apply_fallback_timer` at the end of `on_wifi_apply_creds`. If `LL_EV_WIFI_CONNECTED` doesn't fire within the window, the timer's callback wipes the bad creds via `esp_wifi_restore`, clears `g_sta_ssid` / retry count, and calls a new `bring_up_dev_softap_runtime()` helper that reconfigures the AP and restarts wifi. The existing `WIFI_EVENT_AP_START` handler reposts `LL_EV_WIFI_CONNECTED` so transport / mdns / captive_dns / webapp_assets all come back up just like at boot. `post_wifi_connected` cancels the timer on STA success. Started at 30s for safety; dropped to 15s after Bill's UAT — typical real-network connect is <10s, 15s gives margin without dragging the recovery UX.
    - **Broadened `ll_provisioning_is_active()`**: was narrowly "wifi_prov_mgr is advertising and awaiting creds" (the non-dev path), now reads `esp_wifi_get_mode()` and returns true for AP/APSTA mode. Covers both the dev open-SoftAP and any future wifi_prov_mgr-driven scheme. Backed by a new `g_sta_ssid[33]` cached from `post_wifi_connected`'s `wifi_ap_record_t` and exposed via `ll_provisioning_get_sta_ssid()`. transport's `state_to_json` reads both.

  - **Webapp architectural refactor** ([Firmware/v1/webapp/src/](Firmware/v1/webapp/src/)) — single-page app.tsx exploded into a router with three pages and shared chrome. Hash-based routing per [webapp-spec.md §3.2](docs/webapp-spec.md) — landed now that there are >1 screens (was deferred from Session 4 with a "lands when there's actually >1 screen" note). Files:
    - **[`hooks.ts`](Firmware/v1/webapp/src/hooks.ts)** — `useHashRoute()` reads `window.location.hash`, listens for `hashchange`, returns `[route, navigate]`. Hash routing chosen over history-API because the device's 404 redirect hijacks every path; deep links via path don't survive a refresh.
    - **[`components/Header.tsx`](Firmware/v1/webapp/src/components/Header.tsx)** — title + connection-status badge + per-page nav link ("settings" on /, "back" on /settings, none on /setup). Accepts a `connLabelOverride` prop that swaps the live cycling badge for a static label during the post-submit "switching networks…" view, eliminating a confusing closed/connecting flicker the user shouldn't have to interpret.
    - **[`pages/control.tsx`](Firmware/v1/webapp/src/pages/control.tsx)** — same controls as before (power, pattern, color, brightness) minus the state panel and danger zone (those moved to /settings).
    - **[`pages/settings.tsx`](Firmware/v1/webapp/src/pages/settings.tsx)** — wifi panel ("network: <ssid>", "mode: setup mode (mirror is its own wi-fi)" / "connected to your wi-fi" — replacing the technical "station" jargon Bill flagged), "reconfigure wi-fi" button → /setup, diagnostics dl (the old state panel), danger zone with the now-functional factory reset.
    - **[`pages/setup.tsx`](Firmware/v1/webapp/src/pages/setup.tsx)** — primary affordance is "control mirror now" pinned at the top of the page (Bill's request: easy operation should be the default; provisioning is the secondary task). Below it: SSID + password form with show/hide password toggle, validation hints (WPA2 password 8-64 chars), submit button. After submit: a "credentials sent" view with a live countdown timer matching the firmware's 15s fallback window — at zero, replaces the number with explanatory text ("re-join `LL-Mirror-...` and reload to retry") since the connection is gone by then. Numbered next-steps list explicitly tells the user to switch their wifi to the new network.
    - **[`app.tsx`](Firmware/v1/webapp/src/app.tsx)** — thin router shell. Owns the `MirrorClient` and the global state/conn/err. Auto-routes to `/setup` when first observation has `provisioning_active=true`, to `/` when `provisioning_active=false`. The `autoRouted` guard prevents bouncing the user back to /setup if they manually navigate away while still on the SoftAP. Threads a `provSubmitted` flag down to `Header` for the "switching networks…" override.

  - **Three coupled UX bug fixes (control surface coherence)** based on Bill's UAT — all webapp-side, no firmware changes:
    1. **Power button now reads `state.on` directly** (was `brightness > 0`). pattern_interp gates rendering on `state.on` per [pattern_interp.c L151](Firmware/v1/core/pattern_interp/pattern_interp.c) (`if (!s->on) push_frame(0)`), so brightness alone never lights LEDs — this was the demo-blocking bug Bill caught. Hardware button's off-hold now correctly reflects in the webapp's UI.
    2. **Power-on patches restore `brightness > 0`** when the device was dark. `turnOn()` sends `{on: true, brightness: lastActiveBrightness}` if `state.brightness === 0`; otherwise just `{on: true}`.
    3. **Color / pattern / brightness controls auto-power-on**. `sendColor`, `sendPattern`, and `sendBrightness` (when value > 0) all imply user intent to see something; if `!state.on`, the patch sets `on: true` alongside the field they're changing. `sendBrightness(0)` couples the other way: `{brightness: 0, on: false}`.

  - **Verified end-to-end on real hardware** (XIAO ESP32-C3 on COM3, Bill's Pixel 9 hotspot live, dev PC hopping between SoftAP and hotspot networks via existing wifi profiles):
    - **Build green**: 0x103040 / 0x140000 bytes, 19% free (231KB headroom). Sub-4 added ~5.5KB on top of sub-3 (the timer + handler + state extension + new ops). Webapp bundle: 12.4KB gzipped — 16% of the 80KB budget, ~67KB headroom for V2 settings/auth/telemetry/OTA UI.
    - **Real-network end-to-end**: Bill submitted his actual hotspot credentials via /setup. Countdown displayed 15→0, status badge showed "switching networks…" instead of the cycling closed/connecting flicker. Device joined the hotspot at IP 10.160.147.61, mdns republished. Bill switched dev PC wifi to the hotspot, opened `http://layered-logic-mirror-b2332c.local/` — webapp loaded straight to /control (auto-routed because state showed `provisioning_active=false`). All five capabilities verified on the new netif: power on/off, color presets and picker, pattern dropdown, brightness slider, factory reset (which wiped the creds, brought back the SoftAP, and produced the green-hold LED cue Bill called out as a nice touch).
    - **Failure recovery (the demo-saver)**: garbage SSID + valid-length password → response received in <1s (response-flush delay working) → 15s timeout → device restored SoftAP automatically, visible in PC's wifi scan, get_state showed `provisioning_active=true` again. **A typo'd password no longer strands the device.**
    - **Hardware button × webapp coherence** (post-fix): hardware long-hold off → webapp's power button switched to OFF; webapp click ON → LEDs lit; click a color preset while off → device turned on AND showed the color; drag brightness slider above 0 while off → device turned on at the dragged level. All four directions of the on/off coupling verified.

  - **Bug log updates** ([docs/post-mini-sprint-bugs.md](docs/post-mini-sprint-bugs.md)):
    - **Closed #3** (color/pattern controls don't auto-power-on the device): fixed in this sub-milestone via the webapp-side coupling described above. The deeper #2 (firmware-level `state.on` and `state.brightness` desync) stays open — hardware button modality can still produce `on=true, brightness=0` from button gestures alone — but the user-facing symptom is gone.
    - **Opened #5** (Windows captive sheet → MSN): Windows' "Open browser and sign in" notification opens `https://www.msn.com/?ocid=wispr` instead of the webapp. Two-layer cause: WISPr v2 fallback when no DHCP option 114 is advertised + HSTS preventing HTTP downgrade. Phone path works correctly; dev PC workaround is to navigate manually. Real fix is RFC 8910 captive-portal API via DHCP option 114 (1-2 days), or WISPr v2 XML in the captive probe response (~half day). Deferred to post-demo.
    - **Opened #6** (error flow during failed cred submission): when creds are wrong, webapp can only infer "didn't work" from "didn't get a fresh state broadcast within the timeout." The cred-application path tears down the very network used to talk to the device, so success/failure information generated post-leave is unreachable. **Architecturally intrinsic to SoftAP-based provisioning** — BLE provisioning sidesteps it because the BLE link doesn't depend on wifi state. Punted to post-V1 BLE work; demo-day workaround is the documented "if not reachable in 15-20s, the credentials were wrong" recovery.

  - **Sub-4 closed; sub-5 effectively superseded.** The original sub-5 plan was "retire `LL_DEV_OPEN_SOFTAP=1`, replace dev shortcut with the real flow" — but with our architectural choice in sub-4, **the dev SoftAP path *is* the real flow**. The wifi_prov_mgr-driven SoftAP scheme we'd have substituted in is the option-C path we explicitly rejected (protobuf-over-HTTP for the webapp). What remains in spirit of sub-5 is mostly cleanup: rename the `LL_DEV_OPEN_SOFTAP` macro to drop the `DEV_` prefix, update the inline comments to reflect "this is the production SoftAP for V1 standard variant" instead of "replace before prod", and the dev-only SSID prefix (`LL-Mirror-`) is already the production format. **30 minutes of cleanup, deferable to post-demo without functional impact.** Session 5's [Definition of Done](docs/mini-sprint-app-demo.md) is satisfied today.

  - **Session 5 closed; the webapp half of the May 5 advisor demo is fully functional.** Five sessions banked today (1, 2, 3, 4 + foundation, plus all five Session 5 sub-milestones). Remaining mini-sprint scope: Sessions 6-8 (RN app bootstrap, RN control screens, polish + dry-run). The reusable artifacts from Session 5 land in Session 7's RN port without modification: [protocol.ts](Firmware/v1/webapp/src/protocol.ts) and [ws-client.ts](Firmware/v1/webapp/src/ws-client.ts) are explicitly Preact-free; the new state fields (`provisioning_active`, `wifi_ssid`) and new ops (`set_wifi_creds`, `factory_reset`) are wire-protocol-stable; the on/off + brightness coupling logic in [pages/control.tsx](Firmware/v1/webapp/src/pages/control.tsx) is the canonical client behavior the RN app should match.

- [x] [LL-035-5-5](tasks.md#LL-035-5-5) **Session 5 sub-5: drop the "DEV_" prefix from the SoftAP machinery (Apr 29).** The cleanup pass that closes the original sub-5 scope. Pure rename + comment refresh; no functional changes, no behavior changes, no wire-protocol changes. Done before stopping for the day so Session 6 can start from a clean naming slate.
  - **Identifier renames** in [core/provisioning/provisioning.c](Firmware/v1/core/provisioning/provisioning.c) + [.h](Firmware/v1/core/provisioning/provisioning.h) + [variants/standard/main.c](Firmware/v1/variants/standard/main.c):
    - `LL_DEV_OPEN_SOFTAP` → `LL_SOFTAP_PROVISIONING` (build flag — set to 0 only for future BLE-only or Matter-only variants where SoftAP isn't user-facing)
    - `LL_DEV_AP_*` constants (SSID_PREFIX, CHANNEL, MAX_CONN) → `LL_AP_*`
    - `init_dev_softap` → `init_softap`, `bring_up_dev_softap_runtime` → `bring_up_softap_runtime`, `g_dev_softap_init_done` → `g_softap_init_done`, `on_dev_ap_started` → `on_ap_started`
    - Public `ll_provisioning_kick_dev_softap()` → `ll_provisioning_kick_softap()` (touched the .h + the single caller in main.c)
  - **Comment + log-string refresh**: dropped "DEV-ONLY:" / "DEV mode:" / "Replace before prod" framing from comments + ESP_LOG calls. Provisioning module's top-of-file doc rewritten to describe the V1 surface coherently (first-boot SoftAP path, cred-apply failure path, saved-creds boot path) rather than as "Sessions 1-4 dev shortcut." Removed the standalone post-kick warning log line that pointed at sub-3 since sub-3 has shipped. Cleaned up the `#else` branch of the cred-apply fallback to read as "build flag opted out" rather than "non-dev build not yet implemented."
  - **Adjacent freshness fixes**: [transport.h](Firmware/v1/core/transport/transport.h)'s top-of-file doc updated from "Session 1 scope: ping only" to the actual V1 op surface (`ping`, `get_state`, `set_state`, `set_wifi_creds`, `factory_reset`). [main.c](Firmware/v1/variants/standard/main.c)'s wiring-order comment updated to include the captive_dns + provisioning_kick_softap steps that sub-3 and sub-4 added (was 11 steps, now 14).
  - **Verified on silicon** (XIAO ESP32-C3 on COM3, full erase-flash to test the no-creds path): build green at 0x103140 / 0x140000 (19% free, +0x100 vs sub-4 — the rename added a few bytes of distinct symbol names but is essentially the same code). Boot log all clean post-rename: `provisioning: SoftAP configured ("LL-Mirror-B2332D", ...)` + `provisioning: SoftAP up: ssid=...` + `webapp_assets: registered /  /app.js  /app.css  + 404 captive redirect (12466 bytes gzipped total)` + `transport: listening on :80 (ws /ws)` + `captive_dns: listening on udp :53, hijacking all A queries to 192.168.4.1`. The historical "DEV mode: SoftAP up. Captive-portal flow lands in Session 5" warning is gone — captive portal is here now.
  - **No sprint_log retro-edit of older entries**: prior sub-1/sub-2/sub-3/sub-4 entries continue to reference `LL_DEV_OPEN_SOFTAP` / `init_dev_softap` etc. as historical artifacts. Those describe what was true at write-time; rewriting them would be revisionist.
  - **Session 5 fully closed.** Tomorrow morning starts on Sessions 6-8 from a clean naming slate.

- [x] [LL-035-6](tasks.md#LL-035-6) **Sessions 6-8 collapsed into one morning: bare RN app, all 5 controls, end-to-end against the live mirror over the phone hotspot (Apr 30).** What was scheduled as three sessions (Fri-Sun May 1-3) landed in a single ~2.5-hour morning push — RN scaffold, WS client port, single-screen UI with on/off + color + brightness + pattern + factory reset, all driving the XIAO ESP32-C3 prototype on Bill's Pixel 9 hotspot. Morning started with a hard 3-hour time-box and 4 hard gates (init → port → connect → controls); blew past the gates instead of hitting the 30-min Tier-1 backoff trigger. Demo-day RN gate now satisfied 5 days ahead of the May 5 advisor demo, opening the rest of the week for Right-to-Repair / brand finalization / user interviews — none of which were touched yet this week.
  - **New tree at [App/v1/](App/v1/)** — bare RN project (deliberately not Expo) sibling to Firmware/v1/. Scaffolded via `npx @react-native-community/cli@latest init v1 --skip-install --skip-git-init` from `App/`. Versions: React Native 0.85.2, React 19.2.3, TypeScript 5.5, JDK 25 (Microsoft build), Gradle 9.3.1, AGP+Kotlin pinned by RN 0.85's gradle plugin. Android-only this sprint (iOS still deferred — needs a Mac). Standalone npm project (own [package.json](App/v1/package.json), no monorepo hoisting).
  - **Three Windows-specific build wedges hit and fixed in succession** before the first APK installed:
    1. **`gradlew.bat` not on PATH from git-bash.** RN CLI's `react-native run-android` invokes `gradlew.bat` without the `.\` prefix Windows requires for current-dir batch resolution. Fixed by driving Gradle directly: `cd App/v1/android && .\gradlew.bat app:installDebug` from PowerShell.
    2. **foojay-resolver-convention 0.5.0 incompatible with Gradle 9.3.1.** First gradle invocation crashed with `NoSuchFieldError: Class org.gradle.jvm.toolchain.JvmVendorSpec does not have member field 'IBM_SEMERU'` — that constant was removed in Gradle 9. The plugin pin lives at [App/v1/node_modules/@react-native/gradle-plugin/settings.gradle.kts:16](App/v1/node_modules/@react-native/gradle-plugin/settings.gradle.kts) inside `node_modules`. Bumped `0.5.0 → 1.0.0` in place. **Tech debt**: this patch will get nuked on `npm install` — needs a `patch-package` follow-up or an upstream RN bump (RN 0.86+ presumably). Filed as a follow-up, not blocking the demo.
    3. **Windows MAX_PATH blew up cmake codegen.** ninja encodes the absolute source path into the build-output filename, so the worktree's depth (`...\.claude\worktrees\amazing-goodall-97a8cf\App\v1\node_modules\react-native-safe-area-context\common\cpp\react\renderer\components\safeareacontext\RNCSafeAreaViewShadowNode.cpp.o`) crossed 260 chars and gradle exited with `Filename longer than 260 characters`. Windows long-paths is enabled at the registry level (`HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled=1`) but ninja/cmake don't honor it. Fixed structurally rather than by junctioning: the new App.tsx doesn't actually need `react-native-safe-area-context` (uses `SafeAreaView` from `react-native` core instead), so `npm uninstall react-native-safe-area-context @react-native/new-app-screen` removed both the offending dep and the unused starter-screen lib. After dropping deps, also had to clean `App/v1/android/{app/.cxx, app/build, build}` to flush the stale codegen target reference (`ninja: error: unknown target 'react_codegen_safeareacontext'`). With the deps gone and dirs cleaned, build #4 went green in 44s.
  - **Phase 2 source ports** at [App/v1/src/](App/v1/src/):
    - **[protocol.ts](App/v1/src/protocol.ts)** — copied **verbatim** from [Firmware/v1/webapp/src/protocol.ts](Firmware/v1/webapp/src/protocol.ts). The webapp version was deliberately written framework-free for exactly this port ("Preact-free so the RN app can import it verbatim in Session 7" — line 2 of the webapp original). One micro-tweak: `crypto.randomUUID` cast typed against RN's polyfill shape since RN's TS lib doesn't model it identically to `lib.dom.d.ts`. All 7 PatternId values, DeviceState shape, RequestEnvelope/ResponseEnvelope/StateBroadcast types preserved.
    - **[ws-client.ts](App/v1/src/ws-client.ts)** — copy of [Firmware/v1/webapp/src/ws-client.ts](Firmware/v1/webapp/src/ws-client.ts) with **only the `defaultDeviceUrl()` helper removed** (depends on `import.meta.env.DEV` which doesn't exist in Metro). All other logic — request/response correlation via `Map<req_id, …>`, 5s per-request timeout, exponential reconnect backoff (1s → 2s → 4s → 8s capped), broadcast vs response fork at `op === "state"`, `getState/setState/ping/setWifiCreds/factoryReset` — runs unchanged on Hermes. RN's WebSocket polyfill exposes `OPEN`/`CONNECTING` static constants and the `addEventListener('open'|'close'|'error'|'message')` API the webapp client used, so no shim was needed. Validates the architecture call from Session 3 to keep the WS client framework-free.
  - **Single-screen UI at [App/v1/App.tsx](App/v1/App.tsx)** — wires all 5 demo capabilities. Connection panel (URL input + Connect/Disconnect + open/connecting/closed status pill), Power toggle (single button, label swaps On/Off based on `state.on`), Color (9-swatch grid: red/orange/yellow/green/cyan/blue/indigo/magenta/white), Brightness (4 step buttons at 25/50/75/100), Pattern (7 buttons across 2 rows for the protocol's PatternId enum), and a "Danger" zone with Factory reset (uses RN's native `Alert.alert` for confirmation — no custom modal needed). Brand-aligned: dark `#0B0A0F` ground, `#F4EFE6` cream type, `#3214FF` indigo for active selections — matches the brand-positioning Hybrid palette without pulling in any UI library (sticking to the no-deps rule from the webapp). On WS-open, fires a `getState` to populate controls immediately rather than waiting for a user-driven broadcast (mirrors the snapshot-on-open pattern from webapp Session 3). Factory-reset confirmation copy explicitly warns about Wi-Fi creds being wiped and SoftAP fallback.
  - **Network architecture for the dev loop:** Bill's PC has both ethernet (home LAN, internet) and Wi-Fi joined to "Bills pixel 9" hotspot (10.123.210.0/24) where the mirror lives. Android emulator's NAT routes outbound through the host, and Windows' route table picks the WiFi NIC for 10.123.210.0/24, so the AVD can reach `ws://10.123.210.61/ws` directly (~714ms ICMP round-trip — slow because of the AVD-NAT + WiFi double-hop, but functionally fine). Mirror IP found by ping-sweeping the hotspot /24 then reading `arp -a` (Windows mDNS resolver doesn't work cleanly on multi-homed hosts; the mirror's MAC `f0:9e:9e:b2:33:2c` matches the `LL-Mirror-B2332D` hostname's MAC suffix, off-by-one for STA vs AP per Espressif's adjacent-MAC convention). Default URL in App.tsx hardcoded to `ws://10.123.210.61/ws` for this morning — mDNS discovery via `react-native-zeroconf` was the original Session 7 plan but got dropped from the morning push since the demo doesn't strictly need it (the Apr 22 mini-sprint plan flagged hardcoded IP as the official escape hatch).
  - **Three end-to-end verifications** on the AVD against the live mirror:
    1. **Connect + Ping** (Phase 3 gate): Status pill went `idle → connecting → open` (green) within ~1s of tapping Connect. PING returned `fw=e1b31f9-dirty uptime=362s` — fw_version matches commit `e1b31f9` (Session 5 sub-4 from yesterday; the mirror was last reflashed before sub-5's identifier rename). Round-trip: AVD → emulator NAT → host WiFi NIC → phone hotspot → mirror → response back, with full JSON envelope contract honored.
    2. **set_state via swatch tap** (Phase 4 gate): Tapping the yellow swatch sent `{base_color: "#FFD400"}`, mirror state broadcast came back, swatch border highlighted, hex text below swatches updated to `#FFD400`. Mirror LEDs visibly responded.
    3. **set_state via on/off toggle**: Tapping the Power button sent `{on: false}`, broadcast came back, button label flipped to "Off" and color changed to grey, then second tap restored. The transient `socket closed` lastError text that briefly appears on rapid-fire taps matches the known bug from yesterday's [a410834](https://github.com/) backlog issue #1 — auto-reconnect recovers within 1-8s, conn pill stays green, demo path stays functional. Not new tech debt.
  - **Independent firmware-side verification** mid-session via PowerShell `Net.WebSockets.ClientWebSocket` from the host: device responded to `get_state` with `{on:true, pattern_id:"scanner", base_color:"#FFD400", brightness:75, led_count:66, auth_mode:"open", telemetry_enabled:false, provisioning_active:false, wifi_ssid:"Bills pixel 9"}` — confirming the AVD UI changes really propagate to firmware NVS state and aren't UI-only theatrics. Closes the loop on protocol parity between webapp and RN clients.
  - **Partition impact: zero.** RN app lives entirely on the phone — no firmware delta. The 1.25MB factory-slot headroom (~19% free / 251KB after Session 5 sub-5) is preserved going into Sessions 7-8 stretch goals or Phase 3's PCB / hardware integration in Week 6. The flash-size BOM decision (4MB vs 8MB ESP32-C3-MINI-1) stays open per [project_bom_and_pcb](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/project_bom_and_pcb.md), but no new pressure on it from this morning.
  - **What's NOT in this morning's scope** (intentional, not regression): mDNS discovery via `react-native-zeroconf` (Session 7 stretch — IP hardcoded for now), BLE provisioning (Session 8 stretch goal in the original mini-sprint plan), per-screen navigation / multi-mirror UI (Session 8), and brand polish on typography (Berkeley Mono on Android requires bundling the font asset — out of scope for the demo, monospace fallback used). All cleanly deferable to post-demo since the demo-day Definition of Done from the mini-sprint plan only requires the 5 controls working on the RN side, which they do.
  - **Demo-day Definition of Done from [docs/mini-sprint-app-demo.md §5](docs/mini-sprint-app-demo.md):** webapp end-to-end ✓ (Session 5 closed yesterday), RN app discovers mirror ✓ (via hardcoded IP — escape hatch documented), RN app's 4 controls + factory reset ✓ (this morning). **Cross-client sync** is implicitly verified since both surfaces speak the same `set_state`/broadcast protocol that Session 2 already proved with two simultaneous wscat watchers — explicit "open webapp + RN at same time, change from one, watch the other update" can be a final dry-run on May 4-5 buffer days.
  - **Time accounting:** ~30 min on the gradle / foojay / MAX_PATH wedges (vs the 30-min Tier-1 backoff trigger — came right up to the gate before pivoting from "is this hopeless" to "the failure modes are known, one more clean attempt"); ~10 min on the actual successful build #4 (44s gradle + ~5 min npm install caching from prior runs); ~10 min on Phase 2 source ports (almost free since the webapp client was written portable); ~30 min on Phase 4 wiring + verification (UI + WS round-trips + screenshot-driven AVD inspection). Phase 1's "fight the toolchain" portion was the only real risk; everything downstream of build success was straight-line work.
  - **Stop-the-bleeding for next session:** RN app is committed to `App/v1/`, builds clean, runs on the AVD, talks to the mirror. Next session can start by either (a) bringing the demo-script polish (multi-mirror UI, mDNS auto-discovery, BLE prov stretch goal), or (b) pivoting to the unstarted Week 4 graded HCDE deliverables (Right-to-Repair, brand finalization §1 sentence + mini-brand name, user interviews) — the latter is the higher-leverage call given engineering is now ahead of schedule and the academic deliverables are behind.

- [x] [LL-035-7](tasks.md#LL-035-7) **Afternoon push: RN app provisioning + Find Mirror + brand pass + first install on the physical Pixel 9 (Apr 30).** Took the morning's AVD-only RN scaffold and turned it into a phone-installable demo with a working factory-reset → re-provision → control round-trip. Closes the demo-day "RN app same controls" line on [docs/mini-sprint-app-demo.md §5](docs/mini-sprint-app-demo.md), and crosses what was originally Decision #7 in the mini-sprint plan ("RN app skips BLE / provisioning entirely for V1") since RN now does SoftAP-based provisioning end-to-end without BLE.
  - **Trigger**: morning's RN app worked from the AVD against an already-provisioned mirror, but a factory-reset round-trip exposed a real demo gap. After the RN app's Danger > Factory reset button fires, the mirror returns to SoftAP and the RN app has no way to send creds — workflow forces an awkward switch to the laptop webapp to re-provision. Bill flagged it as a real demo blocker; spec said "webapp does setup, RN does control" but that doesn't survive a factory-reset demo step.
  - **Setup screen + auto-route in [App.tsx](App/v1/App.tsx)** — mirrors the [webapp's setup page logic](Firmware/v1/webapp/src/pages/setup.tsx) without copying its routing machinery (RN app stays single-page). Auto-renders when `state.provisioning_active === true && !setupBypassed`, hides controls until creds submitted or user explicitly bypasses. Form has SSID + password (with show/hide toggle) + WPA2-length validation (empty for open networks, otherwise 8-64 chars). Submit calls the existing `setWifiCreds` op (already in [App/v1/src/ws-client.ts:152](App/v1/src/ws-client.ts:152) — wire op landed Session 5 sub-4). Post-submit shows a 15s countdown matching firmware's `LL_APPLY_FALLBACK_US` and switching-network instructions. "Control directly (skip Wi-Fi setup)" escape hatch lets the user bypass setup and use the SoftAP path for ad-hoc demos.
  - **URL preset row** — was hardcoded `ws://10.123.210.61/ws` from the morning. Replaced "Home" preset with **Find mirror** (subnet scanner, see below), kept **SoftAP** (`ws://192.168.4.1/ws`). User no longer needs to know the mirror's home-network IP — Find Mirror discovers it.
  - **WifiInfo native Kotlin module** at [App/v1/android/app/src/main/java/com/v1/WifiInfoModule.kt](App/v1/android/app/src/main/java/com/v1/WifiInfoModule.kt) + [WifiInfoPackage.kt](App/v1/android/app/src/main/java/com/v1/WifiInfoPackage.kt). RN core has no API to read the device's local Wi-Fi IPv4 address; `WifiManager.connectionInfo.ipAddress` is one line of Kotlin behind a permission. Registered in [MainApplication.kt](App/v1/android/app/src/main/java/com/v1/MainApplication.kt) via the legacy NativeModule pattern (RN 0.85 still supports it; TurboModule + codegen would be more code for the same one getter). Two new permissions in [AndroidManifest.xml](App/v1/android/app/src/main/AndroidManifest.xml): `ACCESS_WIFI_STATE` (required) + `ACCESS_NETWORK_STATE` (defensive, for future ConnectivityManager paths). **Lesson**: deferred `react-native-zeroconf` and `react-native-ble-plx` because both are deep-path deps that hit the Windows MAX_PATH wedge (per [feedback_rn_windows_wedges.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_rn_windows_wedges.md)); a hand-rolled native module avoids that fight while staying scoped to one file.
  - **Subnet-scan discovery** at [App/v1/src/find-mirror.ts](App/v1/src/find-mirror.ts) — replaces mDNS for now. Reads the phone's IP via WifiInfo, computes the /24, fires 253 parallel `fetch('http://X/')` probes with a shared 1.2s deadline, looks for `<title>Layered Logic Mirror</title>` in the response body (the embedded webapp's title is the only LL-specific marker that's stable across reboots), races to first match via promise-resolved-on-first-non-null, returns `ws://<found-ip>/ws`. **Known limitation**: race semantics mean multiple mirrors on the same network return whichever responds first — non-deterministic. Captured as a parked task chip ("user-defined mirror names + multi-mirror picker") that adds a `name` NVS field, surfaces id+name via DeviceState, changes findMirror → findMirrors plural with a UI picker. Not in scope today.
  - **Brand pass + UX polish:**
    - **App label**: `v1` → `LLogic` ([strings.xml](App/v1/android/app/src/main/res/values/strings.xml)). Wordmark plays on the LL of the logo + "logic"; same launcher label for users.
    - **Launcher icon**: nested L's of the Layered Logic logo, generated from the canonical SVG geometry. Built [scripts/gen_app_icon.py](App/v1/scripts/gen_app_icon.py) — a 100-line PIL script that re-renders the polylines from `assets/brand/logo/logo-primary-dark.svg` directly into PNGs at all 5 mipmap densities (mdpi 48 → xxxhdpi 192). Avoids needing inkscape / cairosvg / imagemagick on the dev machine (none installed). Three knobs exposed for tuning: `INNER_INSET_FACTOR` (channel width between nested L's), `PADDING_RATIO` (icon canvas margin), `SW_OUTER`/`SW_INNER` (stroke weights). Iterated three times against Bill's eye on the live device — final values 1.55 / 0.08 / 16 / 14 read clean on the Pixel 9 launcher with themed icons enabled. Background is RGBA-transparent so the launcher's own tile shows through (Pixel 9 was framing the dark `#0B0A0F` square awkwardly).
    - **Title bar overlap**: morning version had "LL Mirror" sitting under the system status bar because `SafeAreaView` from `react-native` core only honors iOS notch insets — Android is a no-op there. Fixed by manually adding `StatusBar.currentHeight` padding to the body container. Title now sits cleanly below the system status bar.
  - **First install on physical hardware** (Pixel 9 Pro, USB tethered with USB debugging on, `adb -s 47081FDAP002TS install -r`). APK ~107MB multi-arch (arm64-v8a + armeabi-v7a + x86 + x86_64); same APK works on AVD too. Metro tunneled over USB via `adb reverse tcp:8081 tcp:8081` so JS bundle fetches don't depend on phone Wi-Fi state — important since the phone leaves Wi-Fi when joining the mirror's SoftAP.
  - **Demo-network setup**: Bill recruited a second phone running its own hotspot ("LayeredLogicDemo") so the Pixel 9 (which used to be the hotspot AP) is now free to act as a Wi-Fi client. Solves the "phone can't be both AP and STA simultaneously" constraint that would otherwise block testing the SoftAP provisioning flow on the same device.
  - **Verified end-to-end on the Pixel 9** in three legs:
    1. **App install + control over existing-mirror network**: APK installed, `adb reverse` set up, app fetched JS bundle from Metro over USB, connected to mirror at the prior `Bills pixel 9` hotspot IP, controls drove LEDs. Bill: "really cool to see the app on my physical device!"
    2. **SoftAP provisioning round-trip**: factory-reset the mirror via the RN app's Danger button → mirror returned to SoftAP `LL-Mirror-B2332D` → Pixel 9 joined that SoftAP via Settings → opened LLogic app → tapped SoftAP preset → tapped Connect → Setup screen auto-rendered (provisioning_active was true) → entered SSID `LayeredLogicDemo` + password into the form on the phone (kept out of this transcript) → tapped Connect to Wi-Fi → submitted state with countdown → mirror tore down SoftAP and joined `LayeredLogicDemo` → DHCP gave it `192.168.235.61`.
    3. **Find Mirror discovery + control on new network**: Pixel 9 switched to `LayeredLogicDemo` → tapped Find Mirror → subnet scan found the mirror at `192.168.235.61` → tapped Connect → controls live on the new network. Bill: "now i can control the mirror from my phone through the app!"
  - **Concrete proof that USB-side debugging worked alongside the test**: `adb logcat` filtered for `ReactNativeJS|FATAL|com.v1` streamed cleanly throughout — saw the SafeAreaView deprecation warning on each cold start (loud but functionally a no-op until we re-introduce `react-native-safe-area-context`, which is blocked on the MAX_PATH wedge), saw `Running "v1" with {"rootTag":1,"initialProps":{},"fabric":true}` confirming Fabric (new architecture) is live, saw zero exceptions across the full provisioning round-trip. Logcat cleared between runs via `adb logcat -c` so each test session has a clean transcript.
  - **Parked / known limitations from this push:**
    - Find Mirror is single-mirror-only by design today (race semantics). Captured as a chip task (user-defined mirror names + multi-mirror picker).
    - `react-native-safe-area-context` is still uninstalled per the morning's MAX_PATH workaround; the deprecation warning persists. A patch-package or upstream RN bump could re-introduce it cleanly.
    - mDNS auto-discovery via `react-native-zeroconf` still deferred — the subnet-scan path covers it for V1, and a phone-side mDNS resolver would be cleaner long-term but adds another deep-path dep.
    - The foojay 1.0.0 patch in [App/v1/node_modules/@react-native/gradle-plugin/settings.gradle.kts](App/v1/node_modules/@react-native/gradle-plugin/settings.gradle.kts) is still in-tree only — `npm install` will wipe it. patch-package or a different mechanism still owed.
  - **Ready for the May 5 advisor demo.** Webapp side closed Apr 29; RN app side closed in two pushes today. Cross-client sync (webapp + RN open simultaneously) is the last DoD checkbox — easy to test by browsing to `http://192.168.235.61/` from the Pixel 9's Chrome alongside the LLogic app, change a control on one surface, watch the broadcast update the other. No code work expected for the cross-client test; it's a verification step Bill can run any time before May 5.

- [x] [LL-035-8](tasks.md#LL-035-8) **Cross-client sync demo verified end-to-end (Apr 30).** Final demo-day Definition-of-Done line from [docs/mini-sprint-app-demo.md §5](docs/mini-sprint-app-demo.md). Bill ran the bench test: webapp loaded on the dev PC + LLogic app open on the Pixel 9, both pointed at the same mirror on `LayeredLogicDemo` hotspot. State changes from either surface propagated to the other within ~broadcast latency. Bill: "I can make changes from the webapp on the dev pc that are seen in the mirror and in the mobile app." Mini-sprint DoD now fully green five days ahead of the May 5 advisor meeting. **Minor follow-up**: noticed the two surfaces have inconsistent color-swatch presets and slightly different hex codes for matching color names — captured as a parked task chip ("Unify webapp/RN color swatches to one source"). Polish-only; doesn't affect the protocol-level sync.

- [x] [LL-036](tasks.md#LL-036) **Right-to-Repair portfolio rationale — three docs landed (Apr 30).** Closes the Week 4 carryover for the HCDE portfolio's repair story. Bill's [right-to-repair-philosophy.md](docs/right-to-repair-philosophy.md) (Apr 20) and [Failure_Modes/](Failure_Modes/) inventory (Apr 27) already covered values + technical evidence; what was missing was the analytical / quantitative / navigational layer that ties them together as a portfolio artifact. Three new docs land that layer without rewriting the existing material:
  - **[docs/repair-design-decisions.md](docs/repair-design-decisions.md)** — 20-row decision matrix mapping specific design choices (Phillips fasteners, friction-fit assembly, commodity LED strip, replaceable controller module, OTA, recessed factory-reset, etc.) to five repair values (Reopenable, Commodity parts, Self-recovery, Long service life, OTA-fixable) and the failure modes each pre-empts. Format is matrix-first because repair claims are easy to make in narrative; the matrix forces every row to terminate at a specific failure-mode link and a specific spec-doc citation. Surfaces three architectural patterns the failure-mode-by-failure-mode files don't make explicit: (1) every controller-side failure terminates at either user-recovery or swap path — no failure strands the buyer, (2) 14 of 17 failure modes resolve via parts that don't depend on Layered Logic, (3) OTA protects against the failure mode the inventory can't enumerate (post-ship software regressions).
  - **[docs/repairability-scorecard.md](docs/repairability-scorecard.md)** — quantitative companion. Top-line: 17 failure modes, 12 fully user-repairable (71%), 5 partial, 0 non-repairable, 1-tool requirement (Phillips), 7-year minimum parts commitment, 6 of 8 component classes 100%-commodity-sourceable. Per-mode table covers severity / repair tier / tools / parts source / estimated repair time; the time column is intentionally **TBD-flagged** pending golden-sample stopwatch measurement, with sourcing notes inline. §5 comparative table is the most-TBD section — needs iFixit / EU-scoring-framework data; the framework is laid out, the cells are placeholders. §6 ties repairability to lifetime carbon amortization (1-yr / 7-yr / 14-yr scenarios from the [sustainability-audit](docs/sustainability-audit.md) data).
  - **[docs/repair-index.md](docs/repair-index.md)** — sub-MOC pulling the repair ecosystem (philosophy + inventory + decisions + scorecard + supporting context: sustainability, brand, BOM, firmware architecture, button rationale) into one navigable artifact. §How-to-read-this-set tells different audiences (buyer / advisor / regulator / engineer / repair shop) which path through the doc set maps to their question. Linked from [index.md](index.md) HCDE Artifacts section so the home note now surfaces the repair sub-MOC as a single entry point rather than five sibling docs.
  - **Cross-platform link discipline**: all cross-references use standard markdown links per the Apr 21 vault convention — render in both Obsidian (vault navigation) and GitHub (public read). No wiki-link syntax. Frontmatter `tags` aligned with existing HCDE artifact tagging.
  - **What's deliberately NOT in this set**: the user-facing repair guide (Week 4 → Week 8 final, blocked on the assembly guide which lives in the operations track), the packaging QR code (blocked Apr 28 awaiting final packaging design), and any user-research integration (interviews still pending). Each is referenced as a known-pending companion in the new docs, not silently skipped.

- [x] [LL-037](tasks.md#LL-037) **Brand-positioning §1 sentence locked (Apr 30).** Closes the carryover from [Apr 20's brand worksheet](docs/brand-positioning.md) — §1 was the only section still in template-Mad-Libs form with a "Your edits: ___" placeholder. Locked text:

  > *"For design-conscious adults who want atmosphere over gadgetry, [product name] is a made-to-order LED light line that blends into a living space as ambient presence and that you can still repair with a screwdriver ten years later — designed, built, and documented as a system to keep, not a novelty to throw away."*

  Drafted 3 candidates (Curator-first / anti-pattern-first / lightly-compressed-from-seed); Bill picked the third — closest to the Apr 20 seed, single-sentence compression, generalized category. Tightening moves: (1) two sentences → one, (2) "made-to-order LED *infinity mirror*" → "made-to-order LED *light line*" so the same statement covers Nixie clocks and future product lines under the Tier 2 mini-brand, (3) differentiator threaded through the predicate rather than split across two clauses. The `[product name]` slot remains bracketed — Tier 2 mini-brand naming is still deferred per the Apr 20 decision; mirror-specific candidates (Depthwise / Inwards / Threshold) ruled out then as too narrow once the parent expanded to multi-line. Original Apr 20 seed kept inside a `<details>` block in the doc for traceability. [project_brand_locked_decisions memory](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/project_brand_locked_decisions.md) updated with the locked sentence.

- [x] [LL-038](tasks.md#LL-038) **OTA infrastructure validated end-to-end on silicon — V1 → V2 swap over Wi-Fi alone (Apr 30).** Late-day push to test the actual OTA mechanism that the [right-to-repair philosophy §3](docs/right-to-repair-philosophy.md#3-what-i-commit-to) commits to ("OTA firmware update support for the life of each unit") and that [firmware-architecture-scoping §5.4](docs/firmware-architecture-scoping.md) specifies for production. Today's scope is the throwaway dev-test path — proves A/B partition boot ordering + esp_https_ota client integration work end-to-end before investing in the production plumbing (signed binaries, Cloudflare Worker + R2, anti-rollback eFuse). New code lands as commit `1b86d5e` (welcome sequence + OTA module + start_ota op) plus a follow-up commit covering the V2 cyan tweak + sdkconfig fix + LLogic OTA UI.
  - **Welcome sequence at boot** — synchronous additive fill in [pattern_interp.c](Firmware/v1/core/pattern_interp/pattern_interp.c) between `led_driver_init` and the render task spinning up. Bright color at 100% brightness, 4 ticks @ 500ms, breakpoints derived from `led_count` so the sequence scales: c3_devkit (66 LEDs) → [15, 32, 49, 66] per Bill's spec; prod_v1_pro (32 LEDs) → [8, 16, 24, 32] auto-derived. Doubles as the OTA-success canary — V1 ships white, V2 ships cyan, the visual difference is the OTA-effect proof.
  - **OTA module at [core/ota/](Firmware/v1/core/ota/)** — minimal HTTP-pull dev-test path. Single public function `ll_ota_start(url)` that wraps `esp_https_ota` (which handles both http:// and https:// despite the name). Header comments explicitly call out the throwaway scope so future-me doesn't mistake this for the production OTA path. New `start_ota` WS op in [transport.c](Firmware/v1/core/transport/transport.c) takes `{url}`, dispatches to `ll_ota_start`. On success the device reboots before the WS response sends — clients see socket close, reconnect, observe new `fw_version` via ping (the standard OTA UX contract).
  - **Bug caught and fixed in-session: `CONFIG_ESP_HTTPS_OTA_ALLOW_HTTP=y` was missing.** First OTA trigger from the LLogic app returned `ESP_ERR_INVALID_ARG`. ESP-IDF's esp_https_ota rejects plain http:// URLs unless that config is explicitly set — the dev-test path uses HTTP (no TLS overhead in the throwaway scope), so we needed the flag. Added to [sdkconfig.defaults](Firmware/v1/sdkconfig.defaults) with a pointer to the production-OTA spec (where this flag flips back off and HTTPS becomes mandatory). Required a re-flash of V1' over USB to land the corrected config; then the OTA cycle ran clean.
  - **LLogic app gains an Update firmware UI** — new `Settings → Firmware update` panel with an OTA URL input pre-populated to `http://192.168.223.176:8000/layered_logic_mirror_standard.bin` (dev PC's address on the LayeredLogicDemo subnet). New `MirrorClient.startOta(url)` method in [App/v1/src/ws-client.ts](App/v1/src/ws-client.ts) handles the success path correctly — when the device reboots the socket dies before any response arrives, so the client treats `socket closed` / `timeout` from start_ota as the success signal rather than an error. UI button cycles through "Update firmware" → "Sending request…" → "Mirror downloading + rebooting…" states.
  - **End-to-end OTA cycle verified on silicon, USB unplugged**:
    1. USB-flash V1' (white welcome + OTA support + HTTP allowed) → mirror reboots, plays white welcome, joins LayeredLogicDemo, gets `192.168.223.61`.
    2. Bill unplugged USB. Mirror runs purely on barrel-jack power.
    3. `python -m http.server 8000` on dev PC at `192.168.223.176`, serving `Firmware/v1/build/layered_logic_mirror_standard.bin` (V2: cyan welcome + same HTTP-allowed config).
    4. Bill opened LLogic on Pixel 9, tapped Update firmware → confirmed → mirror downloaded the binary (~1.2MB), validated, swapped active partition, called `esp_restart()`. WS socket dropped on the phone side as expected.
    5. **Visual confirmation from Bill**: cyan welcome sequence on reboot (vs. white before) — visible end-to-end proof that OTA replaced the running code over Wi-Fi alone.
    6. **Independent ping verification from the host**: `ws://192.168.223.61/ws` returned `{fw_version: "1b86d5e-dirty", uptime_s: 235}` — same commit hash but different binary content; uptime confirms post-OTA reboot timing. Bill: "V2 seems to work."
  - **Binary size at the OTA gate**: 0x1239e0 / 1.25MB slot, **9% free** (~115KB headroom). Adding `esp_https_ota` + `esp_http_client` + `app_update` dependencies grew the binary from Session 5 sub-5's ~80% utilization to ~91% — getting tight, will need either a per-slot resize (steal from the unallocated top 128KB) or a cleanup pass (drop unused IDF components from REQUIRES) before adding more substantial features. Captured as a known-watch item; not blocking today.
  - **LLogic app reorganized: Settings page (Apr 30 follow-up).** Initial OTA UI lived inline at the bottom of the controls page, which got crowded fast. Added simple state-based two-page navigation (no router lib): a `route: 'controls' | 'settings'` state, gear icon (⚙) in the top-right of Controls jumps to Settings, "← Back" returns. Settings page hosts three sub-sections mirroring the [webapp's settings.tsx](Firmware/v1/webapp/src/pages/settings.tsx) shape: **Wi-Fi** (current network + mode + Reconfigure button), **Firmware update** (OTA URL + button), **Danger** (factory reset). Wi-Fi reconfigure shares the SSID/password form with the first-time setup flow — heading + button copy adapts based on whether it's user-driven (`reconfiguringWifi`) or device-driven (`provisioning_active`); manual flow gets a Cancel button instead of "Control directly". On disconnect, settings auto-routes back to Controls so users don't get stranded on a stateless page.
  - **Net firmware deltas this push**: 2 commits (`1b86d5e` + the V2 follow-up), ~250 LOC of new firmware (welcome + OTA module + start_ota op), 1 sdkconfig flag, 1 visual change (white → cyan). LLogic app deltas: ~75 LOC for the OTA UI + Settings page + Reconfigure flow.
  - **Production OTA still parked**. The minimum-viable-OTA work landed today is throwaway code per the module's own header comments. The production path needs: signed binaries (ECDSA P-256, key in offline password manager), self-hosted hosting on `ota.layeredlogic.cc` (Cloudflare Worker + R2), `CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK` (eFuse burn — out of scope this sprint per the demo-build hardware safety rules), auto-revert on health-check failure, staged rollout via device-ID hash buckets. Captured as the next post-demo OTA workstream.
  - **Memory note added**: [feedback_screenshot_size.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_screenshot_size.md) — Bill flagged that I'd been reading native-resolution AVD/phone screencaps (1280x2856) directly through Read, which inflates context. Going forward: resize to ~600px max with PIL before reading, OR use text-based UI checks (uiautomator dump, dumpsys, curl) when no visual is needed.

- [x] [LL-039](tasks.md#LL-039) **User-definable mirror name + multi-mirror discovery (Apr 30, follow-up).** Self-contained subset of the V2 multi-mirror story from [docs/mini-sprint-app-demo.md](docs/mini-sprint-app-demo.md): the Find-mirror flow couldn't disambiguate when multiple devices respond on the same subnet, and the only stable identity surfaced over the wire was the MAC suffix (good for plumbing, bad for users). Adds a user-settable `name` to `ll_state_t` plus a read-only `id` (the same MAC suffix the mDNS hostname and SoftAP SSID already publish) and a tiny `GET /api/info` HTTP discovery endpoint so the RN app can read both in one roundtrip per candidate. End-to-end verified over OTA-only.
  - **Firmware** ([core/state_bus/](Firmware/v1/core/state_bus/), [core/nvs/](Firmware/v1/core/nvs/), [core/transport/](Firmware/v1/core/transport/)) — `ll_state_t.name` (32-char user string, default empty = "fall back to id"), `LL_EV_NAME_CHANGE` event type with matching payload, MAC-suffix cache exposed via `ll_device_id_get()` (read once at `ll_state_bus_init()` from `esp_read_mac(ESP_MAC_WIFI_STA)`, lowercase 6-hex). NVS schema bumped 1→2 and a 33-byte `name` field appended to `ll_nvs_blob_t`; deserializer null-terminates defensively, accepts empty as legal (no "default to X" substitute since empty is the documented "unnamed" signal). Set surface: `set_state` accepts `name` like any other field — no separate `set_name` op (clean diff vs. duplication tradeoff favored extension). Wire: `state_to_json` emits both `id` and `name` so all clients (webapp, RN, future) read identity from the canonical state object.
  - **HTTP `/api/info` endpoint** ([transport.c](Firmware/v1/core/transport/transport.c)) — `{"product":"layered-logic-mirror","id":"...","name":"...","fw_version":"..."}` returned with `Access-Control-Allow-Origin: *`. Registered before `webapp_assets`'s 404→`/` captive-portal redirect so the URI handler wins matching. Lets RN's subnet-scan probe each `.1–.254` candidate with one HTTP request that's both the "is this a mirror" sentinel AND carries the picker's display label, vs. the prior approach of HTTP-probing for a title marker and then needing a second WS roundtrip per match for the rest.
  - **RN app** ([App/v1/src/find-mirror.ts](App/v1/src/find-mirror.ts), [App/v1/App.tsx](App/v1/App.tsx)) — `findMirror` (single race-resolve) → `findMirrors` (returns ALL responders deduped by `id`). App.tsx: zero matches surfaces error, exactly one auto-selects the URL, multiple shows a picker rendering name when set, falling back to id, with the IP shown as a mono-font subtitle. Settings page gains a **Mirror name** section (input + Save button) above Wi-Fi; seeded with current `state.name` whenever the user enters Settings, save button greys when `nameInput === state.name` (no-op suppression).
  - **Webapp** ([Firmware/v1/webapp/src/pages/settings.tsx](Firmware/v1/webapp/src/pages/settings.tsx)) — same rename input shape, same input-vs-state guard, plus `id` and `name` rendered in the diagnostics dump. Webapp build green: 12.86KB gzipped (16% of 80KB budget; ~3KB delta vs. prior).
  - **OTA cycle V2 → V3 verified end-to-end on silicon, USB unplugged**:
    1. Mirror running V2 (`1b86d5e-dirty` from the prior OTA test) on `192.168.29.61`.
    2. `idf.py -DLL_BOARD=c3_devkit build` produced the new image at `f1db077-dirty` (binary 0x123ee0 / 1.25MB slot — same 9% free as V2; the rename feature added zero net binary growth despite touching 5 modules + adding a JSON endpoint).
    3. Stale leftover `python -m http.server` from earlier in the day was hoarding `:8000` and serving the old build directory; killed PID 50032, started fresh in `Firmware/v1/build/`.
    4. Sent `start_ota` over WS with `http://192.168.29.176:8000/layered_logic_mirror_standard.bin` → server log confirmed `192.168.29.61 ... GET /layered_logic_mirror_standard.bin HTTP/1.1 200`. Mirror rebooted.
    5. **Verification**: `GET /api/info` → `{"product":"layered-logic-mirror","id":"b2332c","name":"","fw_version":"f1db077-dirty"}` — endpoint that doesn't exist in V2's binary, proving the new image is running. `ws ping` confirmed `f1db077-dirty` with uptime=92s. `get_state` returned the full state object with the new `id` and `name` fields populated.
    6. **Rename round-trip**: `set_state {name: "Living Room"}` → state broadcast carried `name: "Living Room"`, `get_state` confirmed persistence, `/api/info` re-read showed the new name (single source of truth across both surfaces).
  - **Diagnostic note: NVS schema bump v1→v2 forced device state back to defaults on first boot of the new firmware** — by design, since the v1 blob's layout no longer matches and `ll_nvs_deserialize` returns false on schema mismatch (no migration path until shipped users exist, per [nvs_logic.h](Firmware/v1/core/nvs/nvs_logic.h)). Wi-Fi creds survived (separate NVS namespace owned by `wifi_prov_mgr`), so the device rejoined `LayeredLogicDemo` automatically. Color/brightness/pattern reset to factory defaults — would matter for a real user, but on a test mirror it was the cleanest possible verification of the "first boot of new firmware" path.
  - **Diagnostic gotcha worth recording**: my first OTA attempt looked like a failure — WS ping after the reboot reported the OLD `fw_version` and the dev-PC websockets client closed with "keepalive ping timeout". Real story: **the response was stale because the WS-client kept its TCP socket open across the device reboot, and the first ping after the close hit a half-dead socket on the device side that returned cached app-desc**. Probing `/api/info` directly (an HTTP-only path with no socket reuse) immediately revealed the new firmware was running. Lesson for future OTA verifies: always re-probe via HTTP first, then WS, never trust a single transport's first-after-reboot read.
  - **Net deltas this push**: ~120 LOC of firmware (`name` field + `LL_EV_NAME_CHANGE` + `/api/info` + tests + spec doc), ~80 LOC of TS (`findMirrors` + picker + rename UIs in both apps), 1 NVS schema bump (v1→v2). [docs/control-protocol-spec.md](docs/control-protocol-spec.md) updated: §5 state object now lists `id` (read-only, MAC suffix) and `name` (user-set), §7.2 documents the V1 `GET /api/info` discovery shape.

- [x] [LL-040](tasks.md#LL-040) [LL-041](tasks.md#LL-041) **App release-build connectivity rescue + mDNS discovery + IP-in-state, OTA'd to V3 (May 1).** Standalone-install push that started as "the app won't connect when I open it like a normal app" and ended as a real architectural cleanup. Three independent fixes shipped end-to-end on the XIAO ESP32-C3 prototype + a Pixel phone:
  - **Cleartext-traffic block on release builds.** Root cause: RN's gradle plugin (`AgpConfiguratorUtils.kt`) calls `manifestPlaceholders["usesCleartextTraffic"] = "false"` inside a `finalizeDsl` block, which runs *after* the user's `app/build.gradle` and clobbers any override. Mirror serves only `http://` + `ws://` (no TLS endpoint), so debug builds (cleartext=true) connected fine but every release APK silently dropped the `/api/info` probe and the `/ws` upgrade. First fix attempt (`manifestPlaceholders = [usesCleartextTraffic: "true"]` in `release { }`) did nothing — verified by greping the merged manifest at `app/build/intermediates/merged_manifests/release/processReleaseManifest/AndroidManifest.xml` and finding `cleartextTraffic="false"` still present. Real fix: hardcode `android:usesCleartextTraffic="true"` directly in [AndroidManifest.xml](App/v1/android/app/src/main/AndroidManifest.xml), nuking the placeholder. The plugin's substitution becomes a no-op (no placeholder to substitute) and the manifest wins. Verified post-rebuild: merged manifest now reads `cleartextTraffic="true"`. Tradeoff worth recording: this allows cleartext to anywhere, not just LAN. Android's `network_security_config.xml` would let us scope to specific hosts but doesn't accept CIDR ranges, so for a `192.168.x.x`-only product the global flag is actually the standard pattern.
  - **mDNS-based mirror discovery (replaces 254-parallel subnet scan).** Old [find-mirror.ts](App/v1/src/find-mirror.ts) fired 253 simultaneous `fetch()` calls to `192.168.X.1-254`. Diagnosis: phone was on `192.168.5.61/24` (verified via `adb shell ip route get 192.168.5.229`), mirror at `192.168.5.229` — same /24, scan range correct, but **OkHttp's default dispatcher caps concurrent requests at 64**. The first 64 slots fill with `.1-.64` (all stuck on ~3s ARP timeouts because no host responds), the 1500ms `AbortController` fires before the queue drains far enough to reach `.229`, and the mirror's response never even gets dispatched. Direct `ws://192.168.5.229/ws` worked instantly post-cleartext-fix; "Find mirror" still failed — confirming dispatcher saturation, not connectivity. **Architectural fix**: native [MdnsModule.kt](App/v1/android/app/src/main/java/com/v1/MdnsModule.kt) wrapping Android's `NsdManager`. Discovery for `_layeredlogic._tcp.` (matches what [ll_mdns.c](Firmware/v1/core/ll_mdns/ll_mdns.c) publishes), serialized resolves (single-flight via `AtomicBoolean.compareAndSet` — `NsdManager.resolveService` is documented as not-safe-concurrent on API <30, and we want one code path back to minSdk 24), IPv4-only filter (`Inet4Address` check, since the firmware listens on IPv4 port 80 only), 2.5s discovery window. Find-mirror.ts rewritten to call `Mdns.findMirrors()` then `/api/info` per result for the human-readable name (mDNS TXT carries `variant/version/id/auth` but not `name`). Side cleanups: deleted now-dead [WifiInfoModule.kt] (was the IP-getter for the subnet-scan path), renamed `WifiInfoPackage.kt` → `NativeModulesPackage.kt`. Verified live: tap "Find mirror" → resolves in ~2-3s regardless of subnet topology, vs. the old scan that couldn't even survive a clean LAN.
  - **`ip` field in `DeviceState` broadcast.** Bill flagged it during debugging — the webapp uses `http://layered-logic-mirror-b2332c.local/` so the IP is hidden in the mDNS hostname; in the app there was no way to read it from the state table. Mirrored the existing `g_sta_ssid` pattern in [provisioning.c](Firmware/v1/core/provisioning/provisioning.c): added `g_sta_ip[16]` (max IPv4 dotted-quad + null), populated in `post_wifi_connected` via `snprintf(... IPSTR ...)`, cleared in `post_wifi_disconnected` and the cred-apply fallback path, exposed via new `ll_provisioning_get_sta_ip()` declared in [provisioning.h](Firmware/v1/core/provisioning/provisioning.h). [transport.c::state_to_json](Firmware/v1/core/transport/transport.c) emits `"ip"` alongside `"wifi_ssid"` — null when not on STA. Both protocol.ts files updated (`ip?: string | null`). [App.tsx](App/v1/App.tsx) Settings page now shows `IP: 192.168.5.229` line right under `Network:`.
  - **OTA + flash-config rabbit hole worth logging:** First OTA attempt got `ESP_ERR_INVALID_VERSION`. `/api/info` confirmed mirror was running `f1db077-dirty` (commit f1db077 + dirty changes, the V2 build), my new build was `e48c9dd-dirty` — different version strings. Real cause was deeper: the new build was for `target=esp32` (plain Tensilica ESP32, chip ID `0x0000`), not `esp32c3` (RISC-V, chip ID `0x0005`). Source: the committed [Firmware/v1/sdkconfig](Firmware/v1/sdkconfig) had `CONFIG_IDF_TARGET="esp32"` (legacy state — `sdkconfig` shouldn't even be committed per the foundation commit's "regenerated from defaults"). My first build invocation had `idf.py -DLL_BOARD=c3_devkit set-target esp32c3 build`, which failed during cmake configure (missing `webapp/dist/index.html.gz` — fresh worktree had no `node_modules` in `Firmware/v1/webapp/`). The set-target's sdkconfig write apparently rolled back on cmake failure; the retry was just `idf.py build` with no set-target, which read the committed `sdkconfig` and built for `target=esp32`. esp_https_ota validated the chip ID against the running C3, mismatch, INVALID_VERSION. Fix: `npm install && npm run build` in `Firmware/v1/webapp/` to populate `dist/`, `rm -rf Firmware/v1/build/ Firmware/v1/sdkconfig`, then `idf.py -DLL_BOARD=c3_devkit set-target esp32c3` followed by a `Select-String "^CONFIG_IDF_TARGET=" sdkconfig` verification step (caught it at `esp32c3` this time), then `idf.py -DLL_BOARD=c3_devkit -DPROJECT_VER="ip-state-v3" build`. Bumped `PROJECT_VER` explicitly so the version comparison passes regardless of git state. OTA from `http://192.168.1.98:8000/layered_logic_mirror_standard.bin` (laptop on `.1.x`, mirror on `.5.x` — router routes both ways, confirmed by webapp working in the other direction): one `200` GET, mirror reboots, `/api/info` reports `fw_version: "ip-state-v3"`. Settings page in app now shows `IP: 192.168.5.229` after force-stop+reopen.
  - **Follow-up bug not fixed this session**: app OTA button gets stuck on "Mirror downloading + rebooting" forever after a successful OTA — `App.tsx::startOta` sets `otaState='rebooting'` and nothing transitions it back to `'idle'` when the auto-reconnect lands. Needs a `useEffect` that resets on `conn === 'open'` after `otaState === 'rebooting'`. Workaround for now: force-stop the app. Bill flagged this as acceptable for a non-production build; targeted for next-week's polish pass alongside auth + BLE.
  - **sdkconfig confusion clarified late-session.** Initial diagnosis claimed the bad `target=esp32` value came from a *committed* `Firmware/v1/sdkconfig`. Verified after the OTA succeeded that it isn't committed — `git ls-files` returns empty, [Firmware/v1/.gitignore](Firmware/v1/.gitignore) line 3 already lists `sdkconfig`. The bad file was just my local working copy from the failed first build. So the real fix isn't a delete-or-commit decision — it's **documentation**: future-me (or future-Bill) needs the build invocation contract written down or this rabbit hole repeats. Captured in the new "Building" section of [Firmware/v1/README.md](Firmware/v1/README.md), which now explicitly walks through (a) the webapp `npm install && npm run build` prerequisite for fresh worktrees, (b) the `idf.py -DLL_BOARD=c3_devkit set-target esp32c3` + `Select-String "^CONFIG_IDF_TARGET="` verification step, (c) the `PROJECT_VER` bump for OTA iteration to dodge `ESP_ERR_INVALID_VERSION` on identical version strings, and (d) the chip-target-switching gotcha (`Remove-Item sdkconfig` first; set-target doesn't reliably overwrite a sdkconfig from a previous chip).
  - **Multi-network design doc locked, all §10 decisions made.** [docs/multi-network-design.md](docs/multi-network-design.md) — full spec for storing N saved Wi-Fi networks in NVS, scan-and-pick reconnect, protocol additions (`list/add/remove_wifi_network`, with `set_wifi_creds` kept as a deprecated shim), app UX, ~6 days of implementation. §10 converted from "open questions" to "Locked Decisions" table mid-session, with Bill's calls baked into the affected spec sections:
    - **Q1 priority**: last-used wins (matched recommendation; simplest, matches consumer-device norms).
    - **Q2 cap**: `N_MAX = 4` (overrode recommendation of 8 — Bill's realistic personal-use ceiling: home, work, parents, travel hotspot. ~424 B in NVS. Easy to bump if real usage proves it tight.)
    - **Q3 boot-no-network behavior**: stay in BACKOFF indefinitely, **no auto-SoftAP** (overrode recommendation of auto-SoftAP after 10 min). Bill's reasoning: avoid polluting the space with unnecessary RF when the device is just out of range or the user doesn't intend to re-provision. User must trigger pairing mode via recessed-button hold. Aligns with the broader "less RF noise unless asked" stance.
    - **Q4 remove-while-connected**: proactive disconnect, gated by app-side confirm dialog (overrode recommendation of passive iOS-style behavior). Bill's reasoning: passive reads as a bug; the dialog covers the "didn't mean it" case but if confirmed, respect it immediately.
    - **Q5 `set_wifi_creds` deprecation**: keep through V2 lifetime, remove with the next firmware major (matched recommendation).

    Status frontmatter flipped from "draft — awaiting sign-off" to "spec — implementation-ready, decisions locked May 1." Multi-network firmware work is the proposed Week 6 engineering replacement for the no-longer-real "PCBs arrive" item — original plan assumed a PCB order that's been deferred per [project_pcb_status memory](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/project_pcb_status.md).
  - **Two new cross-cutting product principles saved as feedback memories**, surfaced from the §10 Q3 + Q4 calls — they're non-obvious enough to apply forward without re-derivation:
    - [feedback_rf_minimal_unless_asked.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_rf_minimal_unless_asked.md) — connectivity behaviors default to "stay quiet unless the user explicitly entered this mode." Don't auto-fall-back to broadcasting modes (SoftAP, BLE) on passive timeouts. Will inform any future Wi-Fi reconnect / BLE advertisement / scanning specs.
    - [feedback_respect_explicit_actions.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_respect_explicit_actions.md) — destructive ops (Forget, Delete, Reset) execute immediately + confirm dialog, not iOS-style passive-until-natural-disconnect. The dialog covers ambiguity; if confirmed, respect it now. Diverges from common consumer-OS pattern, deliberately.
  - **`project_app_stack` memory refreshed** for the May 1 state — mDNS now noted as done (was previously "deferred"), three architectural fixes captured, follow-up list updated, and a "don't propose `react-native-zeroconf`" guidance line added (the native NsdManager path is the chosen pattern; the deep-dep alternative is a known wedge).
  - **Net deltas this push**: ~150 LOC of native Kotlin (MdnsModule + Package), ~50 LOC of TS (find-mirror.ts rewrite + protocol.ts + Settings page IP line), ~30 LOC of firmware C (provisioning.c IP cache + transport.c state_to_json), 1 manifest hardcode, 1 dead module deleted (WifiInfoModule), 1 design doc landed and locked (~250 lines), 2 new feedback memories, 1 README rewrite for the firmware build invocation, 1 project memory refreshed, no NVS schema change. Nothing committed yet — bundling next session ahead of the multi-network firmware work.

### In Progress

### Blocked

---

## Week 6 (May 5 – May 9): Hardware Integration & Supply Chain

### Done

### In Progress
- [x] [LL-046](tasks.md#LL-046) Multi-network firmware implementation — done May 7. All 5 design-doc steps + connect_wifi_network bonus op + OTA rollback safety net + status-bar overlap fix + UX polish round, all hardware-verified.
- [x] [LL-050](tasks.md#LL-050) Supply chain map — done May 7. 13-line component table, sourcing cascades, executive risk framing for Milestone 2.
- [x] [LL-073](tasks.md#LL-073) RN app polish: haptics + color wheel — done May 7. Content-encoded haptic per LED pattern, brightness-tracking pulse durations, full-width 512×512 color wheel with top-right preview, touch-follow magnifier bubble, and scroll-lock during drag.

  **Step 1 (NVS layer).** New `Firmware/v1/core/ll_wifi/` module split logic-vs-target the same way `core/nvs/` is: `ll_wifi_logic.{c,h}` carries the `wifi_entry_t` struct (locked at 106 bytes), the in-memory `wifi_list_t`, and the pure-C list ops (find / add / remove / pick_next / sanitize) — host-tested with 27 cases covering insert/update/full/invalid, remove + active_idx fixup across all four positions, last-used-wins priority with recently-failed mask + lowest-index tiebreak, and non-null-terminated-string sanitization. `ll_wifi.{c,h}` carries the ESP-IDF NVS plumbing: per-entry blob keys (`entry_0..entry_3`) so add/remove only rewrites what changed, mutex-guarded singleton, and a separate `ll_wifi_migrate_from_esp_wifi(ssid, password)` hook rather than reading `esp_wifi`'s namespace directly — keeps the dependency arrow one-way (provisioning → ll_wifi). Module compiles clean under `-Wall -Wextra -Wpedantic -Werror`; full host test suite green (110 of 111 — the one failure is a pre-existing `nvs_logic` test asserting `led_count==32` that drifted when the prototype default was bumped to 66, unrelated to this work).

  **Step 2 (provisioning refactor).** `core/provisioning/provisioning.c` now drives STA from `ll_wifi` rather than `esp_wifi`'s saved cred. Five behavioral changes: (1) at boot, `migrate_legacy_cred_if_needed()` runs after `esp_wifi_init` — if `ll_wifi` is empty and `esp_wifi_get_config(STA)` returns a non-empty SSID, the legacy cred is copied into `ll_wifi[0]` per design-doc §5.3; (2) `esp_wifi_set_storage(WIFI_STORAGE_RAM)` is set in init so `esp_wifi`'s NVS namespace becomes vestigial — `ll_wifi` is the only persistence path going forward; (3) `pick_boot_idx()` picks the previously-active entry first, falling back to highest-`last_used` (Step 3 will replace this with the SCANNING / PICKING / BACKOFF state machine); (4) `on_wifi_apply_creds` now writes to `ll_wifi` via `ll_wifi_add` *before* the SoftAP→STA handoff, so creds are persisted even if the radio handoff fails midway, and the apply-creds fallback (`apply_creds_fallback_cb`) removes the failed entry from `ll_wifi` instead of relying on `esp_wifi_restore`; (5) `on_factory_reset` calls `ll_wifi_erase_all()` alongside `esp_wifi_restore()`. New static `g_pending_ssid` tracks the in-flight cred so the fallback knows what to remove; cleared on STA-up. `post_wifi_connected` looks up the connected SSID in `ll_wifi` and stamps `ll_wifi_set_active(idx, esp_timer_get_time())` for the boot-time pick logic. Build-clean for esp32c3 / c3_devkit / standard variant — 1052 of 1052 ninja steps; binary is 0x1255a0 bytes (8% headroom in the app slot). OTA-flashed live onto mirror b2332c at 192.168.5.229 over the existing dev runbook path: `fw_version` flipped `ip-state-v3` → `4c1f888-dirty`, STA auto-reconnected to "IoT" at the same DHCP-leased IP, and all 7 `ll_settings` fields (on/pattern/color/brightness/led_count/auth_mode/telemetry) preserved across the reboot. **Migration shim is real-hardware-verified**: device booted with empty `ll_wifi`, read its legacy STA cred from the `nvs.net80211` namespace via `esp_wifi_get_config`, copied it into `ll_wifi[0]` per design-doc §5.3, and connected without user intervention. Cross-validated via fresh WS `get_state` (transport up, `provisioning_active=false`, `wifi_ssid="IoT"`).

  **Step 3 (state machine).** Replaces the unconditional "retry-same-network forever" loop from Steps 1-2 with the SCANNING → CONNECTING → ONLINE / BACKOFF cycle from design-doc §6. New SM in `provisioning.c`: 5 states (IDLE/SCANNING/CONNECTING/ONLINE/BACKOFF), backoff schedule 5s/15s/60s, 8s connect-watchdog, recently-failed bitmask cleared on transition into SCANNING from BACKOFF (so a one-off fail doesn't permanently shadow an entry — the floor of "cleared on reboot or successful connect" still applies as a stricter guarantee). PICKING is sub-logic in `sm_handle_scan_done()`: builds a not-visible mask from `esp_wifi_scan_get_ap_records`, ORs with recently-failed, picks via `ll_wifi_list_pick_next`. STA_START kicks the SM (boot path) or calls `esp_wifi_connect` directly (apply-creds path); STA_DISCONNECTED branches on SM state — CONNECTING → mark fail + rescan, ONLINE → post DISCONNECTED + rescan; SCAN_DONE feeds the picker; IP_GOT_IP transitions to ONLINE. Factory reset and apply-creds fallback both tear the SM down to IDLE. Boot path simplified — no more `pick_boot_idx`/`apply_entry_to_esp_wifi` at init; `esp_wifi_set_mode(STA) + esp_wifi_start` is enough, the SM kicks itself. **First OTA bricked the live mirror** via stack overflow in `sm_handle_scan_done` — 1.6 KB `wifi_ap_record_t` array on the system event task's ~2.3 KB stack. Recovery via USB flash of the same source post-fix, which heap-allocates the records buffer. Mirror booted clean, SM connected to "IoT" autonomously through SCANNING → PICKING → CONNECTING → ONLINE.

  **OTA rollback safety net.** Bricking the mirror revealed a missing dev safeguard: `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` was off, so a panicking OTA had no automatic recovery — the only path back was USB. Enabling it in `sdkconfig.defaults` plus a new `ll_ota_subscribe()` that calls `esp_ota_mark_app_valid_cancel_rollback()` on the first `LL_EV_WIFI_CONNECTED` after boot. Logic: if a freshly-OTA'd image gets online, it's healthy; if it panics first or never reaches the network, the bootloader notices the unmarked PENDING_VERIFY state on the next reset and rolls back to the previous slot. Wired into `variants/standard/main.c` after the captive-DNS subscriber. Orthogonal to LL-071's eFuse anti-rollback (downgrade-attack defense vs. boot-loop recovery). Bootloader change deployed via USB flash; future OTAs are now self-healing.

  **Step 4 (wire protocol ops).** Three new ops in `core/transport/transport.c` — `list_wifi_networks` (read-only enumeration sorted by last_used desc, includes is_active flag), `add_wifi_network` (insert-or-update by SSID, no connect-switch — adding ≠ joining), `remove_wifi_network` (forget by SSID; if removing the active one, proactively disconnect via the new `ll_provisioning_drop_active()` helper, the SM picks the next eligible saved network or BACKOFF). DeviceState gained `wifi_saved_count` per design-doc §7.5 — count only, the full list lives behind `list_wifi_networks` to keep state broadcasts small. add/remove call `broadcast_state()` directly so connected clients see the count change immediately. `set_wifi_creds` retained as the legacy first-cred entry point for the SoftAP setup flow (per §7.4 — kept working through V2 lifetime). OTA-verified on live mirror at 192.168.5.229: smoke test exercised insert/update/full/invalid/remove/not_found paths plus `wifi_saved_count` round-trip; all 9 cases green. Binary fingerprint flipped `6d3f9bc-dirty` → `57fe584-dirty` (the Step-3+rollback commit was the new HEAD when this build was cut).

  **Step 5 (RN app Settings UI).** Per design-doc §8: replaced the single "Network: <ssid> / Reconfigure Wi-Fi" block in `App/v1/App.tsx` with the multi-network surface. New Settings layout shows the connected SSID + IP, then a "Saved networks" list rendered from `list_wifi_networks` — each row has an active-checkmark indicator and a "Forget" button — followed by an "+ Add a network" button that opens an inline SSID/password form. Add submits via `add_wifi_network` (no connect-switch, per §7.2); Forget submits via `remove_wifi_network` with a confirm dialog gated by `is_active` (per §4.3 — non-active forgets silently, removing the active SSID warns the user the mirror will disconnect). `protocol.ts` gained `WifiNetwork` / `AddWifiNetworkPayload` / `RemoveWifiNetworkResult` types plus `wifi_saved_count` on `DeviceState`; `ws-client.ts` gained `listWifiNetworks` / `addWifiNetwork` / `removeWifiNetwork` thin wrappers. Settings page refetches the list on entry (`useEffect` keyed on `route === 'settings'`) and again whenever `state.wifi_saved_count` changes via broadcast — covers the cross-client case where the webapp adds a network while the RN app is open. Type-check clean against the project's `tsconfig.json` (4 pre-existing DOM-types errors from Apr 30 in unrelated lines). Setup-flow form for first-boot provisioning (`provisioning_active=true`) is unchanged — still uses `set_wifi_creds` per §8.2.

  **Step 6 (hardware verification + bug fix).** Built and installed the release APK on the Pixel 9 (debug-keystore-signed; `installRelease` task). Initial debug install showed the Metro "Unable to load script" error since the JS bundle isn't baked into debug builds without the dev server; release builds dodge that. Manual test was partial green — set_wifi_creds, list, add, forget all worked — but Bill hit a UX gap that the spec's "no auto-SoftAP" rule doesn't recover from cleanly: provisioned bad creds for a second network, forgot the first (good) network, and the mirror was stuck in SCAN→fail→BACKOFF with no easy way back. Recovery was a USB `esptool erase_region 0x9000 0x6000` (the `nvs` partition only — preserves firmware in factory/ota_0/ota_1, wipes `ll_settings` + `ll_wifi` + `esp_wifi` cred). Mirror booted clean to SoftAP for re-provisioning. Bill flagged the bigger feature gap: the spec deliberately omits a "switch to this network" op (§4.2 Q1 — "Adding ≠ joining"), but the dual-network failure mode he hit proves it's needed. Added a new `connect_wifi_network(ssid)` wire op, a `ll_wifi_bump_priority(ssid, ts)` helper that only updates `last_used_us` (so `is_active` doesn't lie until the connect actually lands), and a `ll_provisioning_request_switch()` provisioning helper that handles all SM states — ONLINE disconnects, CONNECTING marks-fail-and-disconnects, BACKOFF cancels the timer and re-scans immediately, SCANNING is a no-op (current scan picks up the bumped priority on its own), IDLE is a no-op (no saved networks). RN app side: a "Connect" button on every non-active row in the Settings saved-networks list, calling the new op. Closes the recovery path Bill hit while staying within the "no auto-SoftAP" frame. Firmware USB-flashed (cable was still attached); APK built and ready to install when the phone reattaches.

  **Status-bar overlap fix.** During the same Step 6 session Bill flagged that the Settings nav icon was sometimes drawn under the Android system status bar — couldn't tap into Settings without force-stopping the app a few times to roll the dice on a layout where it landed below the bar. Tracked it to the module-load-time read of `StatusBar.currentHeight` returning 0 in cold-start races on Android 15 / Pixel 9 (the native value is computed from `WindowInsets`, which aren't delivered until after the activity has laid out). First attempt was `react-native-safe-area-context` — proper RN solution, reactive insets — but the deep worktree path blew the C++ codegen filenames past Windows MAX_PATH, and a `subst`-aliased drive broke `path.relative` on cross-drive lookups inside `@react-native/codegen`. Backed it out and shipped a smaller fix: read `StatusBar.currentHeight` at render time, floor it at 32px (the largest typical Android status bar). Even on stale-zero, the title is guaranteed to land below the system bars. Same outcome, no native deps, no Windows-path risk.

  **connect_wifi_network UX polish.** First hardware exercise of the new Connect button surfaced two issues: tapping had no visible/haptic confirmation, and the WS op was timing out from the client's perspective. Tracked the timeout to a sequencing bug — `op_connect_wifi_network` was calling `ll_provisioning_request_switch()` synchronously, which tears the STA down via `esp_wifi_disconnect` *before* transport has a chance to send the response. The WS socket riding on that STA dies mid-response and the client sees a timeout, even though the firmware actually executes the switch correctly (and successfully falls back to the previous network when the chosen one has bad creds). Refactored to post a new `LL_EV_WIFI_REQUEST_SWITCH` event from the op handler; provisioning subscribes and the handler runs on the state-bus task with the same 250ms response-flush yield `on_wifi_apply_creds` uses. App side: optimistic-clear of `is_active` on tap (Bill's UX call — checkmark drops immediately so the user knows the tap landed), brand-indigo "Switching to X…" banner, 15ms `Vibration.vibrate` buzz for haptic confirmation, and the Settings list-refetch `useEffect` now keys on `state.wifi_ssid` (not just `wifi_saved_count`) so the post-switch settle replaces the optimistic state with truth — the banner clears once the broadcast confirms an active row that matches the connected SSID (covers both success and fallback-to-prior cases). Three small follow-ons after a second hardware pass: (1) `Vibration.vibrate(15)` was crashing the app on tap because `VIBRATE` was missing from AndroidManifest.xml — added the permission + wrapped the call in `try/catch` as defense-in-depth so a future stale install or revoked permission doesn't take out the connect flow; (2) banner copy was misleading ("App will reconnect when it's back" — only true if the phone happens to be on the network the mirror switched TO), reworded to tell the user explicitly to match their phone's Wi-Fi and re-run Find mirror; (3) Find-mirror "no mirror found" error now mentions disabling VPN as the most common cause, since Bill's auto-starting VPN scopes the `/24` scan away from his physical LAN and he keeps re-discovering this the hard way.

### Blocked

---

## Week 7 (May 12 – May 16): Service Blueprinting & The Custom Experience

### Done
- [x] [LL-074](tasks.md#LL-074) **User-flow authoring methodology locked + Stage 1 (Discover) drawn (May 13).** Emerged organically as a drill-down on [LL-053](tasks.md#LL-053): Bill asked whether each service-blueprint stage could be visualized as a Figma-style user flow, and we used Stage 1 as the methodology probe. Six iterations across one session produced both the locked HCDE grammar AND a reusable rendering recipe — both now reference artifacts the remaining nine stages will be authored against.
  - **Methodology decisions locked** in [docs/service-blueprint-flows.md § Conventions](docs/service-blueprint-flows.md): (1) Pain points reframed as user-voice diamonds (`Can I imagine this in my space?` instead of `Pain: scale not conveyed`) — flow becomes predictive of user thought process, not descriptive of system state. (2) Two-layer pain evaluation — pain question → recovery probe (also a user-voice diamond, `Is there a tool to help me visualize it?`) → either rejoins golden path on Yes or terminates at a yellow `Missing: X` callout on No. Each `Missing:` terminal is a concrete build-this backlog item. (3) Orange diamond = friction the user is evaluating; red oval = exit (user gone). The shape/color asymmetry is the story — a flow can have many friction points but only a few exits. (4) Natural cognitive order enforced for sequential question chains; for consumer-purchase Stage 1 the cascade is vibe → visualize → value → trust → timing (aesthetic gut check fires fastest, nobody visualizes a piece in their space before deciding they like the look of it). (5) Recovery is optional, not paired — some pains are genuinely unfixable at this stage (wrong vibe, wrong time) and route directly to red bounce. (6) Modality compression: multiple entry points must differ in user behavior or motivation, not channel — *Etsy listing* and *IG listing* are the same encounter for flow purposes.
  - **Rendering recipe locked** in [docs/user-flow-authoring.md](docs/user-flow-authoring.md). Mermaid ruled out: default bezier curves are too curvy; the `step` curve gives right-angle bends but no port control; the ELK plugin fails to load reliably from CDN and breaks the entire render. Switched to **Graphviz via `@viz-js/viz@3` (WASM bundle from jsdelivr)** with `splines=ortho`, explicit port specifiers on every edge (`source:e -> target:w` for Yes, `source:s -> target:n` for No), per-column `rank=same` groups, and `weight=10` on every happy-path edge to keep the top row aligned. The high-value gotcha — easy to get wrong, hard to debug from output: **in `rankdir=LR`, `rank=same` means same *column* (vertical strip), not same row.** First attempt put the whole happy path in one `rank=same` group, expecting horizontal alignment; instead got every node stacked vertically into one column with top-down primary reading order. Fixed by using per-column rank groups (each pain diamond stacked with its own bounce/recovery/missing in that diamond's column) and letting Graphviz auto-layout handle the happy path. Doc captures the rejected approaches as a debugging log so future-me doesn't relitigate.
  - **Stage 1 (Discover) drawn** in both Mermaid (renders inline in [service-blueprint-flows.md](docs/service-blueprint-flows.md) under GitHub / Obsidian with step curves) and Graphviz DOT (renders strict grid-style in [.preview/stage1.html](.preview/stage1.html) — open in any browser, viz-js loads from CDN, renders to inline SVG, no build step). Five pain diamonds along the golden path (`Does this fit my vibe?` → `Can I imagine this in my space?` → `Is this worth what they're asking?` → `Can I trust this maker?` → `Is now a good time for me to engage?`), each `Yes` continuing right, each `No` dropping down. Q1 and Q5 go straight to red bounce ovals (unfixable at this stage). Q2 / Q3 / Q4 each drop to a recovery probe diamond; the recovery `Yes` is an inverted-L back up to the next golden-path gate, the recovery `No` terminates at a yellow `Missing:` rectangle naming the concrete asset / copy / signal the venture needs to build (in-room scale-anchored photos, pricing rationale transparency, social proof / creator placement).
  - **Memory entries added** so future sessions don't repeat the trial-and-error: [feedback_flow_natural_progression](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_flow_natural_progression.md) (always reorder questions for natural cognitive order before shipping) and [reference_user_flow_authoring](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/reference_user_flow_authoring.md) (pointer to docs/user-flow-authoring.md as the canonical methodology + rendering recipe).
  - **Index updated** — both [service-blueprint.md](docs/service-blueprint.md) and [user-flow-authoring.md](docs/user-flow-authoring.md) now linked from [index.md § HCDE Artifacts](index.md).
  - **What this unlocks:** the remaining nine stage flows (2 — Consider, 2b — Consult custom, 3 — Purchase, 4 — Manufacture & QC, 5 — Fulfill, 6 — Unbox & set up, 7 — Daily use, 8 — Trouble & repair, 9 — Returns, 10 — End-of-life) can now be drafted against a locked grammar and reusable templates. Stage 6 (Unbox & Set Up) is in the doc as a pre-v4 placeholder with a "pending retrofit" marker and will be the first to retrofit against the new grammar when work resumes.

- [x] [LL-053](tasks.md#LL-053) [LL-054](tasks.md#LL-054) **Service blueprint landed, including custom-order deep dive (May 13).** Anchor Week 7 deliverable. [docs/service-blueprint.md](docs/service-blueprint.md) maps the complete customer journey across 10 stages — Discover → Consider → (Consult & co-design for custom) → Purchase → Manufacture & QC → Fulfill → Unbox & Set Up → Daily Use → Trouble & Repair → Returns → End-of-Life — with four lanes per stage (Customer Journey / Frontstage / Backstage / Support Systems & Partners), explicit moments-of-truth callouts, and named fail-point lists at every step. The doc is a synthesis layer over the existing vault rather than a parallel artifact: each stage links into the [stakeholder map](docs/stakeholder-map.md) for who's involved, [Assembly_docs](Assembly_docs/) for production detail, [repair-index](docs/repair-index.md) + [Failure_Modes](Failure_Modes/) catalog for the trouble path, [supply-chain map](docs/supply-chain-map.md) for sourcing, [sustainability audit](docs/sustainability-audit.md) for end-of-life, [button-interface](docs/button-interface.md) + [button rationale](docs/button-design-rationale.md) for Stage 6/7 physical UI, [brand positioning](docs/brand-positioning.md) for tone, and the firmware/app/OTA spec docs for the digital-product surfaces. Custom-order branch ([LL-054](tasks.md#LL-054)) is a dedicated Deep Dive section in the same doc: 10-step flow (inquiry → discovery → concept + price band → design proof → ≤3 free revisions → approval + 50% deposit → manufacture → pre-ship review → balance + ship → standard post-purchase), frontstage/backstage table, three custom-specific moments of truth, five fail-points, five open research questions threaded back into [LL-048](tasks.md#LL-048) end-buyer interview agenda. Cross-cutting system-properties table captures the journey-wide guarantees (works without app, mDNS discovery, OTA + rollback, toolless disassembly, JST-modular electronics, commodity parts, documented from day one, open-source on founder exit). Final summary table compresses all 10 stages to one row each for the portfolio-skim view. Open-actions table surfaces 4 new triageable items not yet tracked: define repair-video pipeline (which RMAs trigger filming, where they live), decide custom-order deposit structure (50% at approval vs. 25%/25% split), define carrier insurance threshold, address gift-giver/recipient address-mismatch at fulfillment — flagged for future LL-NNN promotion, not added to the registry inline. No new dependencies introduced; doc is positioned as feed into [LL-061](tasks.md#LL-061) portfolio case study and as the artifact that proves the HCDE-venture thesis ("system, not gadget") in the Week 10 deliverable.

- [x] [LL-055](tasks.md#LL-055) **Broadcast debounce shipped (May 14) — LL-055 fix #1 of N.** Transport's `broadcast_state()` was firing one WS broadcast per primitive state-bus event — for a 3-field `set_state` that was 3 frames in rapid succession, all carrying nearly-identical state, plus the rapid-click case from [post-mini-sprint-bugs.md #1](docs/post-mini-sprint-bugs.md) where 6-8 color preset clicks each generated their own broadcast. Replaced with a 30ms one-shot timer ([Firmware/v1/core/transport/transport.c](Firmware/v1/core/transport/transport.c)): first call arms the timer + sets an atomic-bool pending flag; subsequent calls within the window are no-ops (the pending broadcast will capture the latest state at fire-time anyway). Timer callback clears the flag and runs the existing broadcast logic (renamed `do_broadcast_state`). Fallback path on timer-start failure: immediate broadcast, so transient timer failures don't silently drop state updates. Thread-safe across state-bus, httpd, and esp_timer tasks via stdatomic.h. **Generalized over the bug-doc's "fix #1" framing:** the doc described coalescing only inside `op_set_state`; this debounce catches both the multi-field-set_state case AND the original rapid-click scenario with one mechanism. **Verified on live mirror at 192.168.5.229** via [`Firmware/v1/scripts/ll_debounce_test.py`](Firmware/v1/scripts/ll_debounce_test.py) (Python WS client): single-field `set_state` produced 1 response + 1 broadcast (expected); 3-field `set_state` produced 1 response + 1 broadcast (was 3 before); 6 rapid color sets at ~30ms intervals produced 6 responses + 2 broadcasts (first 5 sends coalesced into 1; 6th fell outside the prior timer window and got its own — correct burst-coalescing, not blanket "always-1" collapse). Build + OTA + verify cycle ran clean: clean rebuild from a fresh worktree (set-target esp32c3, npm webapp build, idf.py build → 0x1274e0 bytes / 8% headroom on the app partition), OTA-flashed via the standard runbook ([docs/ota-dev-runbook.md](docs/ota-dev-runbook.md)) with the dev HTTP server bound to the LAN IP (192.168.1.98:8000) to coexist with Syncthing's loopback :8000. **Next on [LL-055](tasks.md#LL-055):** bug-doc fix #2 (move broadcast fanout to a separate task / queue — the architecturally-right fix, ~1 day) OR pivot to the resilience scenarios (Wi-Fi drop handling, BLE reconnect, network jitter under load).

- [x] [LL-043](tasks.md#LL-043) [LL-043-1](tasks.md#LL-043-1) **Cold outreach campaign engaged + first batch sent (May 14).** [LL-043](tasks.md#LL-043) ("contact list + outreach") had been carrying a stale in-progress status since Apr 28 with the contact list itself never compiled; today consolidated the campaign artifact at [docs/outreach-list.md](docs/outreach-list.md) and sent 5 cold messages against the daily-cadence target Bill set mid-batch (5/day going forward). **Sends:** Reed Kleinman / Smart Home Solver (`reed@smarthomesolver.com`) — Lane A, strongest reply odds; Matt Perks / DIY Perks (`diyperks@gmail.com`) — Lane B, 4.9M-sub story-aligned long shot; Robert Tait / The Hook Up (`smarthomehookup@gmail.com`) — Lane A, found via site contact button; Lewis / Everything Smart Home (form at everythingsmarthome.co.uk/contact) — Lane A, direct competitor coverage; Joe Barnard / BPS.space (`joe@bps.space`) — founder-philosophy lane (not audience-fit), driven by personal resonance with his "afraid to toss old projects" + "personal engineering projects rant" videos. Prototype photo attached to first four; skipped for Joe (founder-philosophy ask, photo would read as pitch sneak-in). **Tone calibration locked** after the first two sends — Bill's actual edits to my drafts established the pattern: cut spec lists to one anchor concept, drop academic taxonomies (no want/need/cool/problematic framework), no LLC signature on cold mail, peer-tone not aspirant. Memory saved as [feedback_cold_outreach_drafts.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/feedback_cold_outreach_drafts.md) so future drafts default to the tighter style; cadence captured in [project_outreach_cadence.md](.claude/projects/C--Users-bowhi-Desktop-Independent-Study/memory/project_outreach_cadence.md). **Spun out [LL-043-1](tasks.md#LL-043-1)** for the content-creator branch (originally folded under LL-043 which was scoped to end-buyer + cafes); script variants (streamer / smart-home / maker / press), n8n automation outline, and verified-contact research captured in the same doc. **Name correction surfaced:** my earlier "TheBitWit (Kevin Kennedy)" suggestion was a confabulation — channel doesn't exist; removed from Lane C and replaced with Optimum Tech. **Tomorrow's queue:** TechHut (Brandon Hopkins, `brandon@techhut.tv`), Hardware Haven (`info@hardwarehaven.media`, weaker fit), bitluni (Twitter/Mastodon DM — no email), plus 2 more to hit the daily cadence target.

### In Progress

### Blocked

---

## Week 8 (May 19 – May 23): Sustainability Deep Dive & Legal

### Done

### In Progress

### Blocked

---

## Week 9 (May 26 – May 30): Marketing Storytelling & Assets

### Done

### In Progress

### Blocked

---

## Week 10 (Jun 2 – Jun 6): Portfolio Construction

### Done

### In Progress

### Blocked

---

## Week 11 (Jun 9 – Jun 12): Launch Readiness Audit

### Done

### In Progress

### Blocked

---

## Related

- [Index / MOC](index.md)
- [Sprint Plan](sprint_plan.md) — the plan this log tracks against
- [Task Registry](tasks.md) — structured source of truth for task IDs and status
- [Task Event Log](task_log.md) — append-only event history
- [Task Format Spec](docs/task-format-v2.md) — schema, ID rules
- [Advisor Meeting — Apr 21](docs/Advisor-Meetings/advisor-meeting-2026-04-21.md) — most recent check-in
