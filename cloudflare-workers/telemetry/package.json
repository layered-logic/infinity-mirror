{
  "name": "ll-mirror-telemetry",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker accepting telemetry beacons from Layered Logic mirrors.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250515.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
